# Wave A — fix-A1 · MASTER-001 查看範例 handler 修復

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修好 `查看範例` 按鈕點擊後展開 `.field-example-body` 的行為，讓 7 個 CIRCLES Phase-1 step 都能用。

**Architecture:** 一行 handler bug — 2026-04-30 redesign 在 `.field-example-toggle` 與 `.field-example-body` 之間插入 `.circles-field-affordance-spacer`，原 handler 用 `btn.nextElementSibling` 取錯元素。改用 `closest()` 在 `.circles-field-group` 容器內查 `.field-example-body`。

**Tech Stack:** vanilla JS, Playwright

**Worktree:** 從 main 起一個 worktree `wave-a-fix-A1`。

---

### Task 1: TDD red — 寫失敗測試

**Files:**
- Create: `tests/playwright/journeys/audit/master-001-example-toggle.spec.js`

- [ ] **Step 1: 寫測試**

```js
// @ts-check
const { test, expect } = require('@playwright/test');

const BASE_URL = process.env.PMDRILL_BASE_URL || 'http://localhost:4000';

test.describe('MASTER-001 查看範例 inline expansion', () => {
  test('CIRCLES step C1 — 查看範例 click 後 body 展開', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    // 進第一題第一步
    await page.locator('.circles-q-card').first().scrollIntoViewIfNeeded();
    await page.locator('.circles-q-card').first().click();
    await page.locator('.circles-q-confirm-btn').first().click();
    // 等 step C1 渲染
    await page.waitForSelector('.field-example-toggle', { timeout: 5000 });

    const toggle = page.locator('.field-example-toggle').first();
    const group = toggle.locator('xpath=ancestor::*[contains(@class,"circles-field-group")][1]');
    const body = group.locator('.field-example-body').first();

    await expect(body).not.toHaveClass(/open/);
    await toggle.click();
    await expect(body).toHaveClass(/open/, { timeout: 3000 });
    await expect(toggle).toHaveAttribute('aria-expanded', 'true');
  });
});
```

- [ ] **Step 2: 跑測試確認紅燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-001-example-toggle.spec.js \
  --project=Desktop-1280 --reporter=line
```

Expected: FAIL — `.field-example-body` 沒拿到 `.open` class。

---

### Task 2: systematic-debugging grep

- [ ] **Step 1: 全站掃 `nextElementSibling`**

```bash
grep -n "nextElementSibling" public/app.js
```

Expected: 找到 `app.js:2967` 是 example toggle handler。檢查其他出現位置看有無同樣 redesign 隱患（NSM 提示按鈕？hint card？），把結果記下來。

- [ ] **Step 2: 確認 DOM 結構**

```bash
grep -n "field-example-toggle\|field-example-body\|circles-field-affordance-spacer\|circles-field-group" public/app.js | head -20
```

Expected: `circles-field-group` 是欄位容器；`field-example-body` 在容器內某層；`circles-field-affordance-spacer` 在 toggle 後。

---

### Task 3: 修 handler

**Files:**
- Modify: `public/app.js:2967`

- [ ] **Step 1: 改 handler**

把：
```js
var body = btn.nextElementSibling;
if (!body || !body.classList.contains('field-example-body')) return;
```

改成：
```js
var group = btn.closest('.circles-field-group');
var body = group ? group.querySelector('.field-example-body') : null;
if (!body) return;
```

- [ ] **Step 2: 跑測試確認綠燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-001-example-toggle.spec.js \
  --project=Desktop-1280 --reporter=line
```

Expected: 1 passed。

---

### Task 4: regression

- [ ] **Step 1: 跑 audit-master**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/audit-master.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
```

Expected: 至少 434 passed / 0 failed（baseline 434）。

- [ ] **Step 2: 跑 jest**

```bash
npm test 2>&1 | tail -5
```

Expected: 5 suites / 104 tests pass.

---

### Task 5: commit

- [ ] **Step 1: commit 訊息**

```bash
git add public/app.js tests/playwright/journeys/audit/master-001-example-toggle.spec.js
git commit -m "$(cat <<'EOF'
fix(circles): MASTER-001 查看範例 handler 改用 closest 取 example-body

2026-04-30 redesign 在 .field-example-toggle 與 .field-example-body 之間
插入 .circles-field-affordance-spacer，原 nextElementSibling 取到 spacer
而靜默 bail，造成 7 個 Phase-1 step 查看範例都失效。

全站掃描：app.js 其他 nextElementSibling 用法已檢視，無同樣 redesign 隱患。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 2: 回報 director**

回報：commit SHA、測試 pass 數字、`nextElementSibling` 全站掃描結果摘要。
