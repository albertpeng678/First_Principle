# Wave A — fix-A4 · MASTER-015 全站「回首頁」icon 化 + MASTER-021 offcanvas spinner

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:**
- M-015：把 CIRCLES 端 9 處 TEXT「回首頁」按鈕全改成 `ph-house` icon；NSM step 4 修 id 並補一顆 back；NSM gate sub-tab 從 icon+text 改純 icon。
- M-021：offcanvas open 時若 list 還在 fetch，顯示 spinner + 「載入練習記錄中…」；resolve 後再 render list 或 empty。

**Architecture:** M-015 統一 selector class `.btn-home-icon`，沿用既有 `.btn-icon` pattern；尺寸 44×44 token 化（M-008 之 prep）。M-021 在 `loadOffcanvasSessions` 前後切視覺。

**Tech Stack:** vanilla JS, CSS, Phosphor icons, Playwright

**Worktree:** `wave-a-fix-A4`，動 `public/app.js` + `public/style.css`。**不要碰 phase-4 final report**（A5 在動）、**不要碰 form / autosave / 上一步**（A3 在動）、**不要碰 example-toggle handler**（A1 在動）。

---

### Task 1: TDD red

**Files:**
- Create: `tests/playwright/journeys/audit/master-015-021-home-offcanvas.spec.js`

- [ ] **Step 1: 寫測試**

```js
// @ts-check
const { test, expect } = require('@playwright/test');

const BASE_URL = process.env.PMDRILL_BASE_URL || 'http://localhost:4000';

test.describe('MASTER-015 全站「回首頁」icon 化', () => {
  test('CIRCLES Phase 1 nav 用 icon 不用文字', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.locator('.circles-q-card').first().click();
    await page.locator('.circles-q-confirm-btn').first().click();
    await page.waitForSelector('#circles-p1-home');
    const homeBtn = page.locator('#circles-p1-home');
    await expect(homeBtn.locator('i.ph.ph-house')).toHaveCount(1);
    await expect(homeBtn).toHaveAttribute('aria-label', '回首頁');
    await expect(homeBtn).toHaveText(/^\s*$/); // 不含可見文字
  });

  test('NSM step 4 同時有 back + home icon', async ({ page }) => {
    await page.goto(BASE_URL + '/?view=nsm&onboarding=0');
    // 略：用 helper 直接 setState 跳到 step 4，或走完整流程
    await page.evaluate(() => { window.AppState.nsmSubTab = 'nsm-step4'; window.renderNsm && window.renderNsm(); });
    await page.waitForSelector('#btn-nsm-home-nav', { timeout: 5000 });
    await expect(page.locator('#btn-nsm-back')).toHaveCount(1);
    await expect(page.locator('#btn-nsm-home-nav')).toHaveCount(1);
  });

  test('NSM gate sub-tab 回首頁是純 icon', async ({ page }) => {
    await page.goto(BASE_URL + '/?view=nsm&onboarding=0');
    await page.evaluate(() => { window.AppState.nsmSubTab = 'nsm-gate'; window.renderNsm && window.renderNsm(); });
    const gateHome = page.locator('#btn-nsm-home-nav').first();
    if (await gateHome.count() > 0) {
      await expect(gateHome).toHaveText(/^\s*$/);
      await expect(gateHome.locator('i.ph.ph-house')).toHaveCount(1);
    }
  });
});

test.describe('MASTER-021 offcanvas loading', () => {
  test('open 後等 fetch 期間顯示 spinner', async ({ page }) => {
    await page.route('**/api/sessions**', async (r) => {
      await new Promise((res) => setTimeout(res, 800));
      r.continue();
    });
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.click('#btn-hamburger');
    await expect(page.locator('.offcanvas-loading')).toBeVisible({ timeout: 1000 });
    await expect(page.locator('.offcanvas-loading .spinner')).toBeVisible();
    // resolve 後 spinner 消失
    await expect(page.locator('.offcanvas-loading')).toBeHidden({ timeout: 5000 });
  });
});
```

- [ ] **Step 2: 確認紅燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-015-021-home-offcanvas.spec.js \
  --project=Desktop-1280 --reporter=line
```

Expected: 4 tests，FAIL（home button 還是文字、step 4 沒 home-nav id、offcanvas-loading 不存在）。

---

### Task 2: systematic-debugging — 全站「回首頁」清單

- [ ] **Step 1: grep 出所有目標**

```bash
grep -n "回首頁\|btn-home\|home-nav\|navbar-home\|circles-nav-home\|circles-p1-home\|circles-p2-home\|circles-score-home\|circles-final-home\|circles-gate-home\|btn-nsm-back\|btn-nsm-home-nav" public/app.js
```

預期 hits：
- `app.js:2799` `circles-p1-home`（Phase 1 desktop）
- `app.js:2828` `circles-p1-home`（Phase 1 mobile）
- `app.js:3261` `circles-gate-home`（gate phase）
- `app.js:3512` `circles-p2-home`（Phase 2 chat）
- `app.js:3913 / 3918 / 3923` `circles-score-home`（Phase 3 score 三條件分支）
- `app.js:3940` `circles-score-home-btn`（Phase 3 nav top-right）
- `app.js:4135` `circles-final-home`（Phase 4 final report）
- `app.js:5856` `btn-nsm-back`（NSM step 4，需改 id 為 `btn-nsm-home-nav` 並補真正的 back）
- `app.js:5969` NSM gate sub-tab `<i ...> 回首頁`（icon+text，改純 icon）

把 grep 完整輸出貼進 commit message。

- [ ] **Step 2: offcanvas race 全站掃**

```bash
grep -n "loadOffcanvasSessions\|loadHistory\|renderOffcanvas\|offcanvas-list\|offcanvas-loading" public/app.js
```

找出所有可能的 race（offcanvas + history view），同模式套 spinner。

---

### Task 3: M-015 通用 helper

**Files:**
- Modify: `public/app.js`（檔案開頭附近，靠近其他 helper）

- [ ] **Step 1: 寫 helper 函式**

```js
function homeIconBtn(id, opts) {
  opts = opts || {};
  var cls = opts.cls || 'btn-home-icon';
  return '<button class="' + cls + '" id="' + id + '" type="button" aria-label="回首頁" title="回首頁"><i class="ph ph-house"></i></button>';
}
```

- [ ] **Step 2: CSS class**

`public/style.css` 加（沿用 `.btn-icon` 既有 token，但獨立 class 方便位移驗證）：

```css
.btn-home-icon {
  width: 44px;
  height: 44px;
  min-width: 44px;
  min-height: 44px;
  padding: 0;
  border: none;
  background: transparent;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: var(--c-text-1, #1f2937);
  border-radius: 8px;
  transition: background 0.15s ease;
}
.btn-home-icon:hover { background: var(--c-bg-2, #f3f4f6); }
.btn-home-icon:focus-visible { outline: 2px solid var(--c-primary); outline-offset: 2px; }
.btn-home-icon i { font-size: 22px; }
```

---

### Task 4: 全站套 icon

針對每個 grep hit 把字串模板的「回首頁」按鈕改成 `homeIconBtn('<原id>')`。逐處改：

- [ ] line 2799 `'<button class="circles-nav-home" id="circles-p1-home" type="button">回首頁</button>'`
  → `homeIconBtn('circles-p1-home')`
- [ ] line 2828 同款 → `homeIconBtn('circles-p1-home')`
  - 注意：兩處 id 重複是已知（desktop / mobile 條件分支），不要動 id 命名。
- [ ] line 3261 gate 內嵌 link 風 → 替換成 `homeIconBtn('circles-gate-home')`，原 inline style 移除（icon button 已有 css）。
- [ ] line 3512 → `homeIconBtn('circles-p2-home')`
- [ ] line 3913 / 3918 / 3923 → 三條件都改 `homeIconBtn('circles-score-home')`
- [ ] line 3940 → `homeIconBtn('circles-score-home-btn')`
- [ ] line 4135 → `homeIconBtn('circles-final-home')`
- [ ] line 5856 NSM step 4：原 `<button class="btn-icon" id="btn-nsm-back" aria-label="回首頁">` 把 id 改成 `btn-nsm-home-nav`（功能本來就是回首頁），並在它前面新增**真正**的 back 按鈕：
  ```js
  '<button class="btn-icon" id="btn-nsm-back" aria-label="返回上一步" title="返回上一步"><i class="ph ph-arrow-left"></i></button>' +
  homeIconBtn('btn-nsm-home-nav')
  ```
  注意：原本 id 為 `btn-nsm-back` 但 aria-label 是「回首頁」是 bug，本次糾正。
- [ ] line 5969 NSM gate sub-tab：把 `<i class="ph ph-house"></i> 回首頁` 那個按鈕替換成 `homeIconBtn('btn-nsm-home-nav')`（id 與其他 NSM 對齊）。

> **位移驗證**：替換完跑 `tests/playwright/journeys/audit/rwd-visual-gate.spec.js`，PNG snapshot diff 不超過 token 容忍。每處 click handler 一定要保留（所有 `addEventListener('click', ...)` 對應 id 不變）。

---

### Task 5: M-021 offcanvas spinner

**Files:**
- Modify: `public/app.js`（offcanvas open / load 區）
- Modify: `public/style.css`（spinner css）

- [ ] **Step 1: render loading skeleton 換成 spinner**

找 offcanvas open handler：
```bash
grep -n "btn-hamburger\|offcanvas-overlay\|offcanvas-list" public/app.js | head -10
```

在 open handler 開頭加：
```js
var listEl = document.getElementById('offcanvas-list') || document.querySelector('.offcanvas-list');
if (listEl) {
  listEl.innerHTML = '<div class="offcanvas-loading"><div class="spinner"></div><p>載入練習記錄中…</p></div>';
}
```

- [ ] **Step 2: load 完才 render**

`loadOffcanvasSessions`（或對應函式）在 await fetch 之後：
- 成功：`replaceChildren` render list 或 empty `<p>尚無練習記錄</p>`。
- 失敗：`<div class="offcanvas-error"><p>載入失敗，請稍後再試</p><button id="offcanvas-retry" class="circles-btn-secondary">重試</button></div>` + bind retry。

- [ ] **Step 3: CSS spinner**

```css
.offcanvas-loading,
.offcanvas-error {
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  padding: 32px 16px; color: var(--c-text-2);
}
.offcanvas-loading .spinner {
  width: 32px; height: 32px;
  border: 3px solid var(--c-bg-2, #e5e7eb);
  border-top-color: var(--c-primary, #5b21b6);
  border-radius: 50%;
  animation: offc-spin 0.8s linear infinite;
  margin-bottom: 12px;
}
@keyframes offc-spin { to { transform: rotate(360deg); } }
```

---

### Task 6: regression

- [ ] **Step 1: spec 綠燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-015-021-home-offcanvas.spec.js \
  --project=Desktop-1280 --reporter=line
```

Expected: 4 passed。

- [ ] **Step 2: 全 viewport regression**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/audit-master.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/rwd-visual-gate.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
npm test 2>&1 | tail -5
```

Expected: audit-master ≥434/0；rwd-visual-gate 64/0；jest 104/0。

- [ ] **Step 3: M-015 位移驗證截圖**

跑簡易 probe 對 8 個 viewport 截 11 個 home button 出現的頁面（Phase 1/2/3/4 + NSM step 2/3/4/gate + offcanvas）。落 `audit/cycles/2026-04-30/screenshots/m-015-verify/`。

```bash
mkdir -p audit/cycles/2026-04-30/screenshots/m-015-verify/
# 寫 probe script 跑 8 viewport × 主要頁面
```

director 收檔後肉眼比對 before/after。

---

### Task 7: commit

```bash
git add public/app.js public/style.css \
  tests/playwright/journeys/audit/master-015-021-home-offcanvas.spec.js \
  audit/cycles/2026-04-30/screenshots/m-015-verify/
git commit -m "$(cat <<'EOF'
fix(circles,nsm,offcanvas): MASTER-015 全站回首頁 icon 化 + MASTER-021 offcanvas spinner

M-015：CIRCLES 9 處 TEXT 按鈕（Phase 1 nav ×2 / gate / Phase 2 / Phase 3 ×4 / Phase 4）
全改 .btn-home-icon 純 icon（ph-house）；NSM step 4 把 #btn-nsm-back（aria 寫回首頁）
正名為 #btn-nsm-home-nav 並補真正的「← 返回上一步」back 按鈕；NSM gate sub-tab
icon+text 收成純 icon。所有 id 維持原樣 click handler 不動。

M-021：offcanvas open 時先 render .offcanvas-loading（spinner + 載入文字），
fetch resolve 後再 replaceChildren list 或 empty；reject 顯示 .offcanvas-error
+ 重試按鈕。

全站掃描：grep 「回首頁」「btn-home」「nsm-home-nav」全 hit 已逐一處理；
位移驗證截圖 audit/cycles/2026-04-30/screenshots/m-015-verify/ 8 viewport × 11 場景。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] 回報 SHA + 4 個 spec test pass + audit-master / rwd-visual-gate / jest 數字 + 位移驗證截圖路徑。
