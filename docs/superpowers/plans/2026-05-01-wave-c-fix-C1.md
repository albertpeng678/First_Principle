# Wave C — fix-C1 · Tech Debt（M-022 字型 + M-023 hex token）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 收 9 處 font-family 字串成 1 個 token；10 處 inline hex 改 token。純技術債，無視覺差異。

**Architecture:** 加 `--c-font-sans` token，全站 grep 替換。Inline hex grep 配對到既有 `--c-text-1/2/3 / --c-primary / --c-bg-*` token。

**Tech Stack:** CSS variables, Playwright

**Worktree:** `wave-c-fix-C1`，動 `public/style.css` + `public/index.html`（必要時）+ `public/app.js`（inline style hex 替換 — 唯一動 app.js 的位置）。

**Base:** `0f30af7`

---

### Task 1: TDD red

**Files:**
- Create: `tests/playwright/journeys/audit/master-022-023-tokens.spec.js`

- [ ] **Step 1: 寫測試**

```js
// @ts-check
const { test, expect } = require('@playwright/test');
const BASE_URL = process.env.PMDRILL_BASE_URL || 'http://localhost:4000';

test('M-022 — 全站使用 --c-font-sans token', async ({ page }) => {
  await page.goto(BASE_URL + '/?onboarding=0');
  const fontToken = await page.evaluate(() =>
    getComputedStyle(document.documentElement).getPropertyValue('--c-font-sans').trim());
  expect(fontToken).toMatch(/DM Sans/);
});

test('M-022 — body font-family 用 token 不出現殘留 DM Sans 字串', async ({ page }) => {
  await page.goto(BASE_URL + '/?onboarding=0');
  // 量幾個關鍵 selector 的 computed font-family 都帶 DM Sans
  const samples = ['body', '.circles-q-card', 'button.btn'];
  for (const sel of samples) {
    const ff = await page.evaluate((s) => {
      const el = document.querySelector(s);
      return el ? getComputedStyle(el).fontFamily : null;
    }, sel);
    if (ff !== null) expect(ff).toMatch(/DM Sans/);
  }
});

test('M-023 — inline hex 殘留量 < 5 (容許 instrument-serif/grade 等保留)', async ({ page, request }) => {
  const html = await (await request.get(BASE_URL + '/app.js')).text();
  const matches = html.match(/style="[^"]*#[0-9a-fA-F]{3,6}/g) || [];
  expect(matches.length).toBeLessThan(5);
});
```

- [ ] **Step 2: 確認紅燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-022-023-tokens.spec.js \
  --project=Desktop-1280 --reporter=line
```

Expected: 3 tests, 至少 2 個 FAIL（token 沒加、inline hex 太多）。

---

### Task 2: systematic-debugging grep

- [ ] **Step 1: 列 font-family 所有不一致用法**

```bash
grep -nE "font-family\s*:" public/style.css public/app.js | head -30
```

預期 9 處 font-family 字串差異（DM Sans 為主，缺 fallback 順序不一）。

- [ ] **Step 2: 列 inline hex**

```bash
grep -nE "style=\"[^\"]*(color|background)[^\"]*#" public/app.js | head -30
```

預期 ≥10 處 inline `#hex` color/background。

---

### Task 3: 加 --c-font-sans token

**Files:**
- Modify: `public/style.css` `:root`

- [ ] **Step 1: 加 token + 全域套**

在 :root 加（B-1 token 區之後）：
```css
  --c-font-sans: 'DM Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

加全站 base：
```css
body, button, input, textarea, select, p, div, span {
  font-family: var(--c-font-sans);
}
```

- [ ] **Step 2: 收掉殘留 font-family 字串**

對 grep 結果裡每一條 `font-family: 'DM Sans', sans-serif` / `font-family: 'DM Sans, sans-serif'` 等改成 `font-family: var(--c-font-sans)`。`Instrument Serif`（grade letter）保留不動。

---

### Task 4: 替換 inline hex

**Files:**
- Modify: `public/app.js` 多處 inline style

- [ ] **Step 1: 替換策略**

對 grep 結果逐條：
- `color: #5a5a5a` → `color: var(--c-text-2)`
- `color: #1a1a1a` → `color: var(--c-text-1)`
- `color: #8a8a8a` → `color: var(--c-text-3)`（既有 token）
- `color: #5b21b6 / #1a56db` → `color: var(--c-primary)`
- `background: #fff` → `background: var(--c-bg-1, #fff)`（保留 fallback）
- 罕用色（如 grade letter `#137A3D` / 教練總評 `#EEF3FF`）若無對應 token 留著。

- [ ] **Step 2: 確認沒漏改**

```bash
grep -nE "style=\"[^\"]*(color|background)[^\"]*#" public/app.js | wc -l
```

Expected: < 5 條（grade letter、emergency error red 等保留可接受）。

---

### Task 5: 跑測試

- [ ] **Step 1: spec 綠燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-022-023-tokens.spec.js \
  --reporter=line
```

Expected: 3 tests pass。

- [ ] **Step 2: 全套 SIT/UAT/UI-UX × 8 viewport**

```bash
npm test 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/audit-master.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/rwd-visual-gate.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
```

Expected: 三層全綠（jest 104+、audit-master 434+、rwd-visual-gate 64+）。

---

### Task 6: commit

```bash
git add public/style.css public/app.js \
  tests/playwright/journeys/audit/master-022-023-tokens.spec.js
git commit -m "$(cat <<'EOF'
chore(style): MASTER-022+023 字型 / hex token 統一

加 --c-font-sans token，body/button/input 全站套用，9 處 font-family 字串
收成 1 個（Instrument Serif grade letter 保留）。

10 處 app.js inline hex (color/background) 替換為既有 --c-text-* /
--c-primary token。grade letter / 教練總評等罕用色保留。

無視覺差異。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

回報：commit SHA + spec pass + 三層數字 + grep 殘留數。
