# Wave B — fix-B1 · 設計 Tokens（M-008 / M-013 / M-014）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 全站套用設計 token 一次解三個 issue：所有按鈕 ≥44×44（M-008）、3 處對比修到 WCAG AA（M-013）、cards/tabs/items 加 :focus-visible ring（M-014）。

**Architecture:** 在 `public/style.css` `:root` 區段新增 token block。對 base button class、chip / pill class、focus-able elements 一次套規則。以最小改動覆蓋多個 audit 列出的失敗點。

**Tech Stack:** CSS variables, :focus-visible, Playwright

**Worktree:** `wave-b-fix-B1`，僅碰 `public/style.css` + 新 spec。**不要動 app.js**（B-2/B-3 在動）。

**Base:** main `0e4699c`（Wave A 已 merged）

---

### Task 1: TDD red — token spec

**Files:**
- Create: `tests/playwright/journeys/audit/master-008-013-014-tokens.spec.js`

- [ ] **Step 1: 寫測試**

```js
// @ts-check
const { test, expect } = require('@playwright/test');
const BASE_URL = process.env.PMDRILL_BASE_URL || 'http://localhost:4000';

test.describe('M-008 tap target ≥44×44', () => {
  test('home 主要按鈕全 ≥44px 高', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    const selectors = ['.circles-q-card .circles-q-confirm-btn', '.nsm-banner-btn'];
    for (const sel of selectors) {
      const el = page.locator(sel).first();
      if (await el.count() === 0) continue;
      const box = await el.boundingBox();
      expect(box && box.height, sel).toBeGreaterThanOrEqual(44);
    }
  });

  test('login 按鈕 ≥44', async ({ page }) => {
    await page.goto(BASE_URL + '/?view=login&onboarding=0');
    const btn = page.locator('button.btn').first();
    if (await btn.count() > 0) {
      const box = await btn.boundingBox();
      expect(box.height).toBeGreaterThanOrEqual(44);
    }
  });
});

test.describe('M-013 contrast tokens', () => {
  test('--c-text-3 / chip / pill 已調 token', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    const text3 = await page.evaluate(() => getComputedStyle(document.documentElement).getPropertyValue('--c-text-3').trim());
    expect(text3.toLowerCase()).toBe('#525252');
    const chipBg = await page.evaluate(() => getComputedStyle(document.documentElement).getPropertyValue('--c-chip-bg').trim());
    expect(chipBg.toLowerCase()).toBe('#ede9fe');
    const pillBg = await page.evaluate(() => getComputedStyle(document.documentElement).getPropertyValue('--c-pill-active-bg').trim());
    expect(pillBg.toLowerCase()).toBe('#ddd6fe');
  });
});

test.describe('M-014 focus-visible ring', () => {
  test('循環 Tab 後第一張題目卡有 outline', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.evaluate(() => {
      const card = document.querySelector('.circles-q-card');
      if (card) card.focus();
    });
    const outline = await page.evaluate(() => {
      const card = document.querySelector('.circles-q-card');
      if (!card) return '';
      const style = getComputedStyle(card);
      return style.outlineWidth + ' ' + style.outlineStyle;
    });
    expect(outline).toMatch(/2px solid/);
  });
});
```

- [ ] **Step 2: 跑測試確認紅燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-008-013-014-tokens.spec.js \
  --project=Desktop-1280 --reporter=line
```

Expected: 4 tests, 至少 3 個 FAIL（token 還沒加、按鈕還是 36px）。

---

### Task 2: systematic-debugging grep

- [ ] **Step 1: 列出 < 44px 的按鈕 selector**

```bash
grep -nE "min-height:\s*([0-3]?[0-9]|4[0-3])px|height:\s*([0-3]?[0-9]|4[0-3])px" public/style.css | grep -i "btn\|button" | head -30
```

把 hit 寫進 commit message。

- [ ] **Step 2: 找 :root token 區段**

```bash
grep -n "^:root\|^\s*--c-text-3\|^\s*--c-primary\|^\s*--c-bg-1" public/style.css | head -10
```

確認 :root 區段所在行號。

- [ ] **Step 3: 找 chip / pill 既有規則**

```bash
grep -nE "\.chip|\.pill|circles-step-meta|circles-progress-seg-letter" public/style.css | head -15
```

確認哪幾個 class 該套 chip/pill token。

---

### Task 3: 加 token block

**Files:**
- Modify: `public/style.css` `:root` 區段

- [ ] **Step 1: 加 sizing + contrast + focus token**

在 :root 內加：

```css
  /* Wave B 設計 token */
  --btn-touch-min: 44px;
  --btn-pad-x: 16px;
  --btn-pad-y: 10px;
  --btn-radius: 8px;
  --btn-font: 14px;
  --c-text-3: #525252;            /* 原 #a1a1aa, AA 5.74:1 */
  --c-chip-bg: #ede9fe;
  --c-chip-text: #5b21b6;
  --c-pill-active-bg: #ddd6fe;
  --c-pill-active-text: #4c1d95;
  --c-focus-ring: #7c3aed;
```

注意：**`--c-text-3` 已存在**舊定義，要把舊值改掉，不是新增重複 declaration。

---

### Task 4: 套 base button sizing

**Files:**
- Modify: `public/style.css` button base rules

- [ ] **Step 1: 套 min-height / min-width**

加（接在 :root 之後或 button 定義區）：

```css
button.btn,
button.btn-ghost,
.circles-btn-primary,
.circles-btn-secondary,
.circles-btn-ghost,
.circles-q-confirm-btn,
.circles-q-cancel-btn,
.nsm-banner-btn,
.btn-home-icon,
.s-step-tab,
button#btn-export-png,
button#btn-nsm-redefine {
  min-height: var(--btn-touch-min);
  min-width: var(--btn-touch-min);
  padding: var(--btn-pad-y) var(--btn-pad-x);
  border-radius: var(--btn-radius);
  font-size: var(--btn-font);
  box-sizing: border-box;
}

/* icon-only 按鈕保持正方 */
.btn-home-icon,
.btn-icon {
  padding: 0;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}
```

不要動有自己 sizing 的特殊 class（例如 `.rt-tbtn` 工具列按鈕、`.dot` 點點），用更精確 selector。

---

### Task 5: 套 contrast token

- [ ] **Step 1: chip / pill 套 token**

找 `.circles-step-meta`（chip-style）與 `.circles-progress-seg-letter`（pill-style）規則，把硬編色改 token：

```css
.circles-step-meta {
  background: var(--c-chip-bg);
  color: var(--c-chip-text);
}
.circles-progress-seg-letter,
.circles-progress-seg.active .circles-progress-seg-letter {
  /* active 用 pill-active token */
}
.circles-progress-seg.active .circles-progress-seg-letter {
  background: var(--c-pill-active-bg);
  color: var(--c-pill-active-text);
}
```

如果 codebase selector 命名不同，以 grep 結果為準調整。

---

### Task 6: 套 focus-visible ring

- [ ] **Step 1: 加全域 focus-visible utility**

在 style.css 末段加：

```css
/* M-014 focus-visible — 統一 keyboard focus 視覺回饋 */
.circles-q-card:focus-visible,
.circles-mode-card:focus-visible,
.circles-type-tab:focus-visible,
.navbar-tab:focus-visible,
.offcanvas-item:focus-visible,
.history-item:focus-visible,
button:focus-visible,
a.btn:focus-visible {
  outline: 2px solid var(--c-focus-ring);
  outline-offset: 2px;
}

/* 移除 ill-defined :focus rules（避免覆蓋 :focus-visible） */
.circles-q-card:focus,
.circles-mode-card:focus { outline: none; }
```

確認 `.circles-q-card` 等容器已有 `tabindex="0"` 才能 focus。如沒，這次不補（director 另開 ticket）。

---

### Task 7: 跑測試

- [ ] **Step 1: spec 綠燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-008-013-014-tokens.spec.js \
  --reporter=line
```

Expected: 4 tests pass。

- [ ] **Step 2: 全 SIT/UAT/UI-UX × 8 viewport（標準提升）**

```bash
npm test 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/audit-master.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/rwd-visual-gate.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
```

Expected: jest ≥104/0; audit-master ≥434/0; rwd-visual-gate ≥64/0。任何 regression 必修。

---

### Task 8: commit

```bash
git add public/style.css tests/playwright/journeys/audit/master-008-013-014-tokens.spec.js
git commit -m "$(cat <<'EOF'
fix(style): MASTER-008+013+014 設計 token — 觸控/對比/焦點環

加 token block：--btn-touch-min/pad-x/pad-y/radius/font (44px) +
--c-text-3 (#525252 從 #a1a1aa) + --c-chip-bg/text + --c-pill-active-bg/text +
--c-focus-ring。對 .circles-btn-* / .nsm-banner-btn / .btn-home-icon /
.s-step-tab 等套統一 sizing。chip/pill 套對比 token 過 AA。
.circles-q-card/.mode-card/.type-tab/.navbar-tab/.offcanvas-item/
.history-item :focus-visible 加 outline 2px。

全站掃描：style.css 中 < 44px button rules 已逐一檢視；硬編色改 token。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

回報：commit SHA + spec 4 tests 結果 + jest/audit-master/rwd-visual-gate 三層數字 + grep 摘要。
