# Wave A — fix-A2 · MASTER-006 + MASTER-009 RWD layout

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 收掉 Mobile-360 的 9px 水平溢出（M-006），並讓 sticky `下一步` bar 不再蓋住 form 中段（M-009）。

**Architecture:** 純 CSS。M-006 對三個 selector 加 `max-width: 100vw; box-sizing: border-box`；M-009 在 form 容器加 `padding-bottom: var(--circles-stickybar-pad)` token。

**Tech Stack:** CSS, Playwright

**Worktree:** `wave-a-fix-A2`，僅碰 `public/style.css`。**不要動 app.js**（A1/A3/A4/A5 在改）。

---

### Task 1: TDD red

**Files:**
- Create: `tests/playwright/journeys/audit/master-006-009-rwd.spec.js`

- [ ] **Step 1: 寫測試**

```js
// @ts-check
const { test, expect } = require('@playwright/test');

const BASE_URL = process.env.PMDRILL_BASE_URL || 'http://localhost:4000';

async function gotoStepI(page) {
  await page.goto(BASE_URL + '/?onboarding=0');
  await page.locator('.circles-q-card').first().click();
  await page.locator('.circles-q-confirm-btn').first().click();
  await page.waitForSelector('#circles-p1-submit');
  await page.click('#circles-p1-submit'); // C1 → I
  await page.waitForSelector('[data-step="I"]', { timeout: 5000 }).catch(() => {});
}

test.describe('MASTER-006 Mobile-360 horizontal overflow', () => {
  test.use({ viewport: { width: 360, height: 720 } });

  test('step I 沒有水平捲軸', async ({ page }) => {
    await gotoStepI(page);
    const overflow = await page.evaluate(() => ({
      sw: document.documentElement.scrollWidth,
      cw: document.documentElement.clientWidth,
    }));
    expect(overflow.sw).toBeLessThanOrEqual(overflow.cw);
  });
});

test.describe('MASTER-009 sticky bar 不蓋 form', () => {
  test.use({ viewport: { width: 375, height: 667 } });

  test('iPhone-SE step I 捲到底時 sticky bar 不重疊最後 textarea', async ({ page }) => {
    await gotoStepI(page);
    await page.evaluate(() => window.scrollTo(0, document.body.scrollHeight));
    const overlap = await page.evaluate(() => {
      const bar = document.querySelector('.circles-submit-bar');
      const tas = Array.from(document.querySelectorAll('textarea'));
      if (!bar || tas.length === 0) return null;
      const barTop = bar.getBoundingClientRect().top;
      const lastBottom = tas[tas.length - 1].getBoundingClientRect().bottom;
      return lastBottom - barTop; // <=0 means no overlap
    });
    expect(overlap).not.toBeNull();
    expect(overlap).toBeLessThanOrEqual(0);
  });
});
```

- [ ] **Step 2: 確認紅燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-006-009-rwd.spec.js \
  --reporter=line
```

Expected: 兩個 test 都 FAIL（M-006 sw 369>360；M-009 overlap 為正數）。

---

### Task 2: systematic-debugging grep

- [ ] **Step 1: 全站掃 width:.*vw**

```bash
grep -nE "width:\s*100vw|right:\s*-?\d+px|min-width:\s*[0-9]" public/style.css
```

把結果存到 commit message 的「全站掃描」段。重點觀察 `.btn.btn-ghost`、`.offcanvas-overlay`、`.circles-submit-bar` 是否有 `100vw` / 缺 `box-sizing`。

- [ ] **Step 2: 量現行 sticky bar 高度**

```bash
node -e "console.log('measure via DevTools');" # 用 playwright 跑：
PMDRILL_BASE_URL=http://localhost:4000 node -e "
const { chromium } = require('playwright');
(async () => {
  const b = await chromium.launch();
  const c = await b.newContext({ viewport: { width: 375, height: 667 } });
  const p = await c.newPage();
  await p.goto('http://localhost:4000/?onboarding=0');
  await p.locator('.circles-q-card').first().click();
  await p.locator('.circles-q-confirm-btn').first().click();
  await p.waitForSelector('.circles-submit-bar');
  const h = await p.evaluate(() => document.querySelector('.circles-submit-bar').getBoundingClientRect().height);
  console.log('submit bar height =', h);
  await b.close();
})();
"
```

Expected: ~72-88px。token 取 `96px` 留 safe-area 緩衝。

---

### Task 3: 修 CSS

**Files:**
- Modify: `public/style.css`

- [ ] **Step 1: 加 token**

在 `:root { ... }` 區段（檔案開頭附近 `--c-primary` 等 token 旁）加：

```css
  --circles-stickybar-pad: 96px;
```

- [ ] **Step 2: 收 Mobile-360 overflow**

找這三個 selector 改／補規則：

```css
.btn.btn-ghost,
.offcanvas-overlay,
.circles-submit-bar {
  max-width: 100vw;
  box-sizing: border-box;
}
```

如果原本有 `width: 100vw` 沒 `box-sizing: border-box`，補上；如果有負 margin / right 配合 padding 撐爆容器，把該 padding 移到內層或加 `overflow-x: hidden` 在父層 `.circles-step-form-wrap`。

- [ ] **Step 3: 加 form 底 padding**

找 `.circles-step-form-wrap`（或實際 form 容器 selector），加：

```css
.circles-step-form-wrap {
  padding-bottom: var(--circles-stickybar-pad);
}
@media (min-width: 1024px) {
  .circles-step-form-wrap {
    padding-bottom: 64px;
  }
}
```

對 Phase-2 chat 容器 `.circles-phase2-chat` 與 NSM step form `.nsm-step-form` 套同 token。

- [ ] **Step 4: 跑 spec 確認綠燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-006-009-rwd.spec.js \
  --reporter=line
```

Expected: 2 passed。

---

### Task 4: regression

- [ ] **Step 1: rwd-visual-gate 不可退化**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/rwd-visual-gate.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
```

Expected: 64 passed / 0 failed。

- [ ] **Step 2: audit-master 全綠**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/audit-master.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
```

Expected: ≥434 passed / 0 failed。

---

### Task 5: commit

- [ ] **Step 1: commit**

```bash
git add public/style.css tests/playwright/journeys/audit/master-006-009-rwd.spec.js
git commit -m "$(cat <<'EOF'
fix(rwd): MASTER-006 Mobile-360 水平溢出 + MASTER-009 sticky bar form 重疊

加 --circles-stickybar-pad token (96px mobile / 64px desktop)；對
.btn.btn-ghost / .offcanvas-overlay / .circles-submit-bar 收
max-width: 100vw + box-sizing: border-box；form / chat / nsm-step
容器補底 padding。

全站掃描：style.css 中所有 width:100vw/right:-px/min-width 規則皆已檢視。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 2: 回報** SHA、spec pass、rwd-visual-gate / audit-master 數字、grep 摘要。
