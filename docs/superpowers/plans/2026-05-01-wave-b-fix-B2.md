# Wave B — fix-B2 · Sticky / Fold Pattern（M-002 / M-003）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 解 iPhone-SE 展開題目卡 sticky 確認鈕被推出 fold（M-002）+ Phase-2 結論預覽 ← 繼續對話 / 確認提交 sticky 失效（M-003）。共用 `position: sticky; bottom: 0` pattern。

**Architecture:** 兩處 DOM 結構小調 + CSS 統一 sticky pattern。`.circles-q-card-expand-area`（題目卡展開區）改成 flex column scroll container，內側 action row 套 sticky；`.circles-conclusion-box` 同理。共用既有 `--circles-stickybar-pad` token（Wave A 設過 96px）。

**Tech Stack:** CSS sticky positioning, env(safe-area-inset-bottom), Playwright

**Worktree:** `wave-b-fix-B2`，動 `public/app.js`（DOM 結構小調，套 wrapper class）+ `public/style.css`。**不要動 token 規則**（B-1 在動）、**不要動 phase-4 final report**（B-3 在動）。

**Base:** main `0e4699c`

---

### Task 1: TDD red

**Files:**
- Create: `tests/playwright/journeys/audit/master-002-003-sticky.spec.js`

- [ ] **Step 1: 寫測試**

```js
// @ts-check
const { test, expect } = require('@playwright/test');
const BASE_URL = process.env.PMDRILL_BASE_URL || 'http://localhost:4000';

test.describe('M-002 展開題目卡 sticky 確認鈕', () => {
  test.use({ viewport: { width: 375, height: 667 } });

  test('iPhone-SE 展開題目卡時 確認按鈕在 fold 內', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    const card = page.locator('.circles-q-card').first();
    await card.click(); // 展開
    const btn = page.locator('.circles-q-confirm-btn').first();
    await btn.waitFor({ state: 'visible', timeout: 5000 });
    const bottom = await btn.evaluate(el => el.getBoundingClientRect().bottom);
    const vh = await page.evaluate(() => window.innerHeight);
    expect(bottom).toBeLessThanOrEqual(vh);
  });
});

test.describe('M-003 Phase-2 結論預覽 sticky 行動列', () => {
  for (const vp of [
    { name: 'iPhone-SE', width: 375, height: 667 },
    { name: 'Desktop-1280', width: 1280, height: 800 },
  ]) {
    test('結論預覽 action row 在 fold 內 — ' + vp.name, async ({ page }) => {
      await page.setViewportSize({ width: vp.width, height: vp.height });
      await page.goto(BASE_URL + '/?onboarding=0');
      // 直接走 setState 跳到 conclusion-expanded
      await page.evaluate(() => {
        window.AppState.circlesPhase = 2;
        window.AppState.circlesSubmitState = 'expanded';
        window.AppState.circlesDrillStep = 'R';
        if (window.renderCirclesPhase2) window.renderCirclesPhase2();
      });
      const submit = page.locator('#circles-conclusion-submit');
      await submit.waitFor({ state: 'visible', timeout: 5000 });
      const bottom = await submit.evaluate(el => el.getBoundingClientRect().bottom);
      const vh = await page.evaluate(() => window.innerHeight);
      expect(bottom).toBeLessThanOrEqual(vh);
    });
  }
});
```

- [ ] **Step 2: 確認紅燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-002-003-sticky.spec.js \
  --reporter=line
```

Expected: 3 tests, FAIL（鈕在 fold 下 9-97px）。

---

### Task 2: systematic-debugging grep

- [ ] **Step 1: 確認 selector + DOM 結構**

```bash
grep -nE "circles-q-card-expand-area|circles-q-card-full-block|conclusion-actions" public/app.js public/style.css | head -10
```

確認：
- 題目卡展開：`.circles-q-card-expand-area`（display:none → block）內含 `.circles-q-card-full-block` + drillPracticeHtml + 確認/取消按鈕（line 1692-1702）。
- Phase-2 結論：`.circles-conclusion-box`（line 3561）→ `.conclusion-actions` 包 `← 繼續對話 / 確認提交`。

- [ ] **Step 2: 確認 stickybar-pad token**

```bash
grep -n "\-\-circles-stickybar-pad" public/style.css
```

Expected: Wave A 已設 96px (mobile) / 64px (desktop)。

---

### Task 3: M-002 題目卡 sticky

**Files:**
- Modify: `public/app.js:1692-1702`
- Modify: `public/style.css`

- [ ] **Step 1: 把 action row 包 wrapper class**

把：
```js
'<div class="circles-q-card-expand-area" style="display:none">' +
  '<div class="circles-q-card-full-block">' +
    '<div class="circles-q-card-full-label">完整題目</div>' +
    '<div class="circles-q-card-full-text">' + escHtml(q.problem_statement || '') + '</div>' +
  '</div>' +
  drillPracticeHtml +
  '<div style="display:flex;align-items:center;gap:8px;margin-top:10px">' +
    '<button class="circles-q-confirm-btn">確認，開始練習</button>' +
    '<button class="circles-q-cancel-btn">取消</button>' +
  '</div>' +
'</div>' +
```

改成：
```js
'<div class="circles-q-card-expand-area scroll-container" style="display:none">' +
  '<div class="scroll-body">' +
    '<div class="circles-q-card-full-block">' +
      '<div class="circles-q-card-full-label">完整題目</div>' +
      '<div class="circles-q-card-full-text">' + escHtml(q.problem_statement || '') + '</div>' +
    '</div>' +
    drillPracticeHtml +
  '</div>' +
  '<div class="action-row">' +
    '<button class="circles-q-confirm-btn">確認，開始練習</button>' +
    '<button class="circles-q-cancel-btn">取消</button>' +
  '</div>' +
'</div>' +
```

- [ ] **Step 2: CSS 套 sticky pattern**

加：
```css
.scroll-container {
  display: flex;
  flex-direction: column;
}
.scroll-container .scroll-body {
  flex: 1 1 auto;
  overflow-y: auto;
  padding-bottom: 8px;
}
.scroll-container .action-row {
  position: sticky;
  bottom: 0;
  background: var(--c-bg-1, #fff);
  border-top: 1px solid var(--c-bg-3, #e5e7eb);
  padding: 12px 16px calc(12px + env(safe-area-inset-bottom));
  display: flex;
  gap: 8px;
  align-items: center;
  z-index: 10;
  box-shadow: 0 -2px 8px rgba(0, 0, 0, 0.04);
}

/* 題目卡展開時限制高度好讓內側 sticky 生效 */
.circles-q-card-expand-area.scroll-container {
  max-height: calc(100dvh - 120px); /* 預留 navbar + card top */
}
```

---

### Task 4: M-003 結論預覽 sticky

**Files:**
- Modify: `public/style.css:2106-2120` 附近 `.circles-conclusion-box` 規則

- [ ] **Step 1: 把 conclusion-box 設 flex column scroll，把 conclusion-actions 套 sticky**

```css
.circles-conclusion-box {
  display: flex;
  flex-direction: column;
  max-height: calc(100dvh - 200px);  /* 上方 navbar / progress / context bar */
  overflow: hidden;
}
.circles-conclusion-box > .conclusion-title,
.circles-conclusion-box > .conclusion-sub,
.circles-conclusion-box > .conclusion-example-block,
.circles-conclusion-box > .rt-field,
.circles-conclusion-box > #circles-conclusion-hint {
  flex: 0 0 auto;
}
.circles-conclusion-box > .rt-field {
  flex: 1 1 auto;
  overflow-y: auto;
}
[data-view="circles"] .circles-conclusion-box .conclusion-actions {
  position: sticky;
  bottom: 0;
  background: var(--c-bg-1, #fff);
  border-top: 1px solid var(--c-bg-3, #e5e7eb);
  padding: 12px 16px calc(12px + env(safe-area-inset-bottom));
  display: flex;
  gap: 8px;
  z-index: 10;
  box-shadow: 0 -2px 8px rgba(0, 0, 0, 0.04);
}
```

- [ ] **Step 2: 桌面 ≥1024 view 確認**

`@media (min-width: 1024px)` desktop 的 conclusion-box 高度約束放寬：

```css
@media (min-width: 1024px) {
  .circles-conclusion-box {
    max-height: calc(100dvh - 240px);
  }
}
```

---

### Task 5: 跑測試

- [ ] **Step 1: spec 綠燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-002-003-sticky.spec.js \
  --reporter=line
```

Expected: 3 tests pass。

- [ ] **Step 2: 全 SIT/UAT/UI-UX × 8 viewport**

```bash
npm test 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/audit-master.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/rwd-visual-gate.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
```

Expected: 三層全綠。

---

### Task 6: commit

```bash
git add public/app.js public/style.css \
  tests/playwright/journeys/audit/master-002-003-sticky.spec.js
git commit -m "$(cat <<'EOF'
fix(circles): MASTER-002+003 sticky/fold pattern — 題目卡 / 結論預覽

題目卡展開區改 .scroll-container > .scroll-body + .action-row 結構，
action row 套 position: sticky; bottom: 0；max-height calc(100dvh - 120px)
讓 sticky 能在 iPhone-SE 生效。

Phase-2 結論盒 .circles-conclusion-box 改 flex column max-height +
.conclusion-actions sticky bottom；rt-field flex:1 overflow-y auto；
桌面 ≥1024 max-height 放寬。

兩處共用 padding: 12px 16px calc(12px + env(safe-area-inset-bottom))，
box-shadow 0 -2px 8px rgba(0,0,0,0.04) 強化分隔。

全站掃描：兩處外，未發現其他「sticky 失效」場景；既有 stickybar-pad
token (Wave A 96px/64px) 仍生效於 .circles-step-form-wrap。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

回報：commit SHA + 3 tests + 三層數字。
