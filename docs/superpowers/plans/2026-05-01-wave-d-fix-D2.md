# Wave D — fix-D2 · drill 限制 + 完成鼓勵 + nav home icon

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:**
- D-3：drill mode 移除「上一步」按鈕（Wave A M-012 加的，與 drill「個別步驟訓練」語意衝突）。同時在完成 drill step 看 score 頁加 encourage card 引導使用者試完整步驟。
- D-4：`.circles-nav` flex row 沒給 home icon 靠右 → home icon 加 `margin-left:auto` + 圓底背景，全站 nav 統一視覺（Phase-1/2/3/4 + NSM）。

**Architecture:** 動 `public/app.js`（drill prev 條件 + encourage card render + handler）+ `public/style.css`（home icon CSS 升級 + encourage card CSS）+ 1 支新 spec。

**Tech Stack:** vanilla JS, CSS, Playwright

**Worktree:** `wave-d-fix-D2`，動 `public/app.js` + `public/style.css` + 1 spec。**不要動 simulation copy / 錯誤訊息卡 / 字型 token**（D1 在動）。

**Base:** main `3d24b7d`

---

### Task 1: TDD red

**Files:**
- Create: `tests/playwright/journeys/audit/master-D3-D4.spec.js`

- [ ] **Step 1: 寫測試**

```js
// @ts-check
const { test, expect } = require('@playwright/test');
const BASE_URL = process.env.PMDRILL_BASE_URL || 'http://localhost:4000';

test.describe('D-3 · drill mode 不能跨步', () => {
  test('drill mode step I 沒有 #circles-p1-prev 按鈕', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.evaluate(() => { localStorage.setItem('circlesMode', 'drill'); });
    await page.reload();
    // 進 drill step I
    await page.locator('.circles-q-card').first().click();
    // drill 預設 step C1，模擬點 step I
    await page.evaluate(() => {
      window.AppState.circlesDrillStep = 'I';
      if (window.renderCirclesPhase1) {
        const main = document.querySelector('#main') || document.body;
        main.innerHTML = window.renderCirclesPhase1();
        window.bindCirclesPhase1 && window.bindCirclesPhase1();
      }
    });
    await expect(page.locator('#circles-p1-prev')).toHaveCount(0);
  });

  test('simulation mode step 1 仍有 #circles-p1-prev', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.evaluate(() => { localStorage.setItem('circlesMode', 'simulation'); });
    await page.reload();
    await page.locator('.circles-q-card').first().click();
    await page.evaluate(() => {
      window.AppState.circlesSimStep = 1;
      if (window.renderCirclesPhase1) {
        const main = document.querySelector('#main') || document.body;
        main.innerHTML = window.renderCirclesPhase1();
        window.bindCirclesPhase1 && window.bindCirclesPhase1();
      }
    });
    await expect(page.locator('#circles-p1-prev')).toBeVisible();
  });
});

test.describe('D-3 · drill 完成 score 頁有 encourage card', () => {
  test('drill mode score 頁渲染 .drill-encourage-card', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.evaluate(() => {
      window.AppState.circlesSession = { id: 'test', mode: 'drill', drill_step: 'I' };
      window.AppState.circlesMode = 'drill';
      window.AppState.circlesDrillStep = 'I';
      window.AppState.circlesPhase = 3;
      window.AppState.circlesStepScore = {
        I: { totalScore: 82, dimScores: {}, comments: { summary: '用戶分群清楚' } },
      };
      if (window.renderCirclesStepScore) {
        const main = document.querySelector('#main') || document.body;
        main.innerHTML = window.renderCirclesStepScore();
        window.bindCirclesStepScore && window.bindCirclesStepScore();
      }
    });
    await expect(page.locator('.drill-encourage-card')).toBeVisible({ timeout: 3000 });
    await expect(page.locator('#drill-encourage-go')).toBeVisible();
    await expect(page.locator('#drill-encourage-dismiss')).toBeVisible();
  });

  test('simulation mode score 頁不渲染 encourage card', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.evaluate(() => {
      window.AppState.circlesSession = { id: 'test', mode: 'simulation' };
      window.AppState.circlesMode = 'simulation';
      window.AppState.circlesSimStep = 1;
      window.AppState.circlesPhase = 3;
      window.AppState.circlesStepScore = { I: { totalScore: 82 } };
      if (window.renderCirclesStepScore) {
        const main = document.querySelector('#main') || document.body;
        main.innerHTML = window.renderCirclesStepScore();
      }
    });
    await expect(page.locator('.drill-encourage-card')).toHaveCount(0);
  });
});

test.describe('D-4 · home icon 視覺修', () => {
  test('Phase-2 chat navbar home icon 有 margin-left:auto computed', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.evaluate(() => {
      window.AppState.circlesSession = { id: 'test', mode: 'simulation' };
      window.AppState.circlesPhase = 2;
      window.AppState.circlesDrillStep = 'R';
      if (window.renderCirclesPhase2) {
        const main = document.querySelector('#main') || document.body;
        main.innerHTML = window.renderCirclesPhase2();
      }
    });
    const icon = page.locator('#circles-p2-home').first();
    await expect(icon).toBeVisible({ timeout: 3000 });
    const ml = await icon.evaluate(el => getComputedStyle(el).marginLeft);
    expect(ml).toBe('auto');
  });

  test('home icon 有圓底（border-radius 50%）', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.evaluate(() => {
      window.AppState.circlesSession = { id: 'test', mode: 'simulation' };
      window.AppState.circlesPhase = 2;
      if (window.renderCirclesPhase2) {
        document.body.innerHTML = window.renderCirclesPhase2();
      }
    });
    const icon = page.locator('.btn-home-icon').first();
    if (await icon.count() > 0) {
      const br = await icon.evaluate(el => getComputedStyle(el).borderRadius);
      expect(br).toMatch(/^(50%|22px)/);
    }
  });
});
```

- [ ] **Step 2: 跑測試確認紅燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-D3-D4.spec.js \
  --project=Desktop-1280 --reporter=line
```

Expected: 6 tests，至少 4 個 FAIL（drill 仍有 prev 按鈕、無 encourage card、home icon 沒 margin-left:auto / border-radius:50%）。

---

### Task 2: systematic-debugging grep

- [ ] **Step 1: drill prev 條件**

```bash
grep -n "circles-p1-prev\|isFirstDrillStep\|showPrev" public/app.js | head -10
```

Expected: Wave A fix-A3 加的條件約 line 2752 附近。

- [ ] **Step 2: step score render**

```bash
grep -n "renderCirclesStepScore\|bindCirclesStepScore\|circlesStepScore\b" public/app.js | head -10
```

Expected: function 約 line 3854 附近。

- [ ] **Step 3: nav 結構 + home icon CSS**

```bash
grep -n "circles-nav {" public/style.css
grep -n "btn-home-icon\|btn-icon" public/style.css | head -10
grep -n "homeIconBtn(" public/app.js | head -10
```

Expected: `.circles-nav` line 1450；`.btn-home-icon` Wave A 已定義；`homeIconBtn(id)` helper Wave A 加。

---

### Task 3: D-3 移 drill 上一步

**Files:**
- Modify: `public/app.js`（grep 找到的 showPrev 條件）

- [ ] **Step 1: 改條件**

把 Wave A fix-A3 寫的：
```js
var isFirstDrillStep = AppState.circlesSession && AppState.circlesSession.mode === 'drill' && stepIdx === 0;
var showPrev = !isFirstDrillStep && stepIdx > 0;
```

改成：
```js
// D-3：drill mode 是個別步驟訓練，不允許跨步；僅 simulation 顯示上一步
var showPrev = AppState.circlesSession && AppState.circlesSession.mode === 'simulation' && stepIdx > 0;
```

---

### Task 4: D-3 drill 完成 encourage card

**Files:**
- Modify: `public/app.js`（renderCirclesStepScore + bindCirclesStepScore）

- [ ] **Step 1: 加 helper**

在 `renderCirclesStepScore` 之前新增：

```js
function buildDrillCompleteEncourageHtml() {
  if (!AppState.circlesSession || AppState.circlesSession.mode !== 'drill') return '';
  return '<div class="drill-encourage-card">' +
    '<h4>🎯 想要完整體驗 PM 思考流程？</h4>' +
    '<p>你剛完成單一步驟，做得不錯！要不要試試 <strong>完整步驟訓練</strong>？25-35 分鐘走完 7 步驟，看完整框架如何串成一個解決方案。</p>' +
    '<div class="encourage-actions">' +
      '<button id="drill-encourage-go" type="button">試試完整步驟訓練 →</button>' +
      '<button id="drill-encourage-dismiss" type="button">下次再說</button>' +
    '</div>' +
  '</div>';
}
```

- [ ] **Step 2: 整合進 renderCirclesStepScore**

找 `renderCirclesStepScore` 的最終 return 字串，在 score detail 區塊與 submit bar 之間插：

```js
buildDrillCompleteEncourageHtml() +
```

需確認在 drill mode 時插入位置在 score 詳情下方、submit bar 上方。

- [ ] **Step 3: handler**

`bindCirclesStepScore`（或對應 binding 函式）加：

```js
document.getElementById('drill-encourage-go')?.addEventListener('click', function() {
  var q = AppState.circlesSelectedQuestion;
  try { localStorage.setItem('circlesMode', 'simulation'); } catch (e) {}
  AppState.circlesMode = 'simulation';
  AppState.circlesSession = null;
  AppState.circlesPhase = 1;
  AppState.circlesSimStep = 0;
  AppState.circlesStepDrafts = {};
  AppState.circlesFrameworkDraft = {};
  AppState.circlesStepScore = null;
  if (q) {
    AppState.circlesSelectedQuestion = q;
    if (typeof triggerCirclesAutoSave === 'function') triggerCirclesAutoSave();
  } else {
    AppState.view = 'circles';
  }
  if (typeof render === 'function') render();
});

document.getElementById('drill-encourage-dismiss')?.addEventListener('click', function() {
  document.querySelector('.drill-encourage-card')?.remove();
});
```

- [ ] **Step 4: CSS**

style.css 末段加：

```css
.drill-encourage-card {
  background: linear-gradient(135deg, #ede9fe 0%, #fef3c7 100%);
  border: 1px solid #c4b5fd;
  border-radius: 12px;
  padding: 16px;
  margin: 16px 0;
}
.drill-encourage-card h4 {
  margin: 0 0 6px;
  font-size: 14px;
  color: #4c1d95;
  font-weight: 600;
}
.drill-encourage-card p {
  margin: 0 0 12px;
  font-size: 12.5px;
  color: var(--c-text-2, #525252);
  line-height: 1.6;
}
.drill-encourage-card .encourage-actions {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}
.drill-encourage-card .encourage-actions button {
  min-height: 44px;
  padding: 10px 14px;
  border-radius: 8px;
  font-size: 12px;
  cursor: pointer;
  box-sizing: border-box;
  font-family: inherit;
}
.drill-encourage-card #drill-encourage-go {
  background: var(--c-primary, #5b21b6);
  color: #fff;
  border: none;
  font-weight: 500;
}
.drill-encourage-card #drill-encourage-go:hover {
  background: #4c1d95;
}
.drill-encourage-card #drill-encourage-dismiss {
  background: #fff;
  color: var(--c-text-2, #525252);
  border: 1px solid #d4d4d8;
}
.drill-encourage-card #drill-encourage-dismiss:hover {
  background: #f3f4f6;
}
```

---

### Task 5: D-4 home icon 視覺修

**Files:**
- Modify: `public/style.css`（補 nav title flex + .btn-home-icon 升級）

- [ ] **Step 1: 加 nav title 撐開**

style.css 末段加：

```css
/* D-4 — nav title block 撐到底，把 home icon 推到右 */
[data-view="circles"] .circles-nav > div:not([class]):first-of-type,
[data-view="circles"] .circles-nav > div:not(.circles-nav-back):not(.circles-nav-right):first-of-type {
  flex: 1 1 auto;
  min-width: 0;
  overflow: hidden;
}
[data-view="circles"] .circles-nav .circles-nav-title,
[data-view="circles"] .circles-nav .circles-nav-sub {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* D-4 — home icon 視覺強化（圓底 + primary 色 + margin-left:auto） */
.btn-home-icon {
  margin-left: auto !important;
  background: rgba(91, 33, 182, 0.06);
  border: 1px solid rgba(91, 33, 182, 0.18);
  border-radius: 50%;
  color: var(--c-primary, #5b21b6);
  flex-shrink: 0;
}
.btn-home-icon:hover {
  background: rgba(91, 33, 182, 0.12);
  border-color: rgba(91, 33, 182, 0.32);
}
.btn-home-icon:focus-visible {
  outline: 2px solid var(--c-primary, #5b21b6);
  outline-offset: 2px;
}
```

`!important` 用來覆蓋既有 `.btn-home-icon` 預設 transparent bg（Wave A）。

---

### Task 6: 跑全套測試

- [ ] **Step 1: spec 綠燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-D3-D4.spec.js \
  --reporter=line
```

Expected: 6 pass。

- [ ] **Step 2: 全套 SIT/UAT/UI-UX × 8 viewport**

記得起自己 worktree-local server (PORT=4333+)：
```bash
npm test 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4333 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/audit-master.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4333 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/rwd-visual-gate.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
```

Expected: 三層全綠（jest ≥107 / audit-master ≥434 / rwd-visual-gate ≥64）。

- [ ] **Step 3: M-012 Wave A 對應 spec 不退化**

```bash
PMDRILL_BASE_URL=http://localhost:4333 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-011-012-form.spec.js \
  --reporter=line
```

注意：Wave A 加的 `M-012 step I 有上一步按鈕` 在 drill mode 現在會 FAIL（這是預期），需要去 spec 把 drill 改 simulation 場景或改斷言。如改 spec，commit message 註明。

---

### Task 7: commit

```bash
git add public/app.js public/style.css \
  tests/playwright/journeys/audit/master-D3-D4.spec.js \
  tests/playwright/journeys/audit/master-011-012-form.spec.js
git commit -m "$(cat <<'EOF'
fix(circles,style): Wave D fix-D2 — drill 移 prev / 完成鼓勵 / nav home icon 視覺修

D-3：drill mode 是個別步驟訓練，移除 Wave A M-012 加的上一步按鈕；
僅 simulation 顯示。drill mode 完成 score 頁加 .drill-encourage-card
鼓勵使用者試完整步驟，click 後切 simulation mode 用同一題開新 session。

D-4：.circles-nav title block flex:1 + ellipsis 推 home icon 靠右；
.btn-home-icon 加 margin-left:auto + 圓底 (rgba(91,33,182,0.06)) +
primary 色 icon + hover 加深，全站 nav (Phase-1/2/3/4 + NSM) 統一視覺。

Wave A master-011-012-form.spec.js 對應 drill 場景改 simulation
(spec 同步更新)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

回報：commit SHA + spec pass + 三層數字 + Wave A spec 是否動到。
