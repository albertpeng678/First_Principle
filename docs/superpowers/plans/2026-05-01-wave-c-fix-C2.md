# Wave C — fix-C2 · Polish (M-024)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** M-024 八個 P2 polish 項：4 個有視覺 (A 桌面 step pill / B iPhone-SE 上一步重點折疊 / C NSM step 4 寬度 / D login 忘記密碼次要化) + 4 個 spec-only (progress label 文字 / hint copy / conclusion-back-btn 確認 / meta wrap)。

**Architecture:** `public/app.js` 多處 string template + `public/style.css` 多處 selector 微調。

**Tech Stack:** vanilla JS, CSS, Playwright

**Worktree:** `wave-c-fix-C2`，動 `public/app.js` + `public/style.css` + 新 spec。**不要動 token**（C-1 在動）、**不要動 auth**（C-3 在動）。

**Base:** `0f30af7`

---

### Task 1: TDD red

**Files:**
- Create: `tests/playwright/journeys/audit/master-024-polish.spec.js`

- [ ] **Step 1: 寫測試**

```js
// @ts-check
const { test, expect } = require('@playwright/test');
const BASE_URL = process.env.PMDRILL_BASE_URL || 'http://localhost:4000';

test.describe('M-024-A · Desktop home 顯示 step pill', () => {
  test.use({ viewport: { width: 1440, height: 900 } });
  test('drill mode 卡片顯示 .circles-info-step pill', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    // drill mode 才有；確認 desktop ≥1280 也看得到
    await page.evaluate(() => { window.localStorage.setItem('circlesMode', 'drill'); });
    await page.reload();
    const pillCount = await page.locator('.circles-info-step').count();
    expect(pillCount).toBeGreaterThan(0);
  });
});

test.describe('M-024-B · iPhone-SE step E 上一步重點預設折疊', () => {
  test.use({ viewport: { width: 375, height: 667 } });
  test('step E 進入時 prev card 是收起狀態', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.evaluate(() => {
      window.AppState.circlesSession = { mode: 'simulation' };
      window.AppState.circlesSimStep = 5; // E step
      window.AppState.circlesPhase = 1;
      if (window.renderCirclesPhase1) window.renderCirclesPhase1();
    });
    const prevCard = page.locator('[data-prev-card], .circles-prev-card-collapsible').first();
    if (await prevCard.count() > 0) {
      await expect(prevCard).toHaveAttribute('open', { timeout: 1000 }).catch(async () => {
        // either details/summary closed OR data-collapsed="true"
        const collapsed = await prevCard.evaluate(el => el.getAttribute('data-collapsed') === 'true' || !el.hasAttribute('open'));
        expect(collapsed).toBe(true);
      });
    }
  });
});

test.describe('M-024-C · NSM step 4 desktop 寬度', () => {
  test.use({ viewport: { width: 2560, height: 1440 } });
  test('Desktop-2560 寬度 ≥ 80% viewport', async ({ page }) => {
    await page.goto(BASE_URL + '/?view=nsm&onboarding=0');
    await page.evaluate(() => { window.AppState.nsmSubTab = 'nsm-step4'; window.renderNsm && window.renderNsm(); });
    const wrap = page.locator('.nsm-step4-desktop, .nsm-step4-wrap').first();
    if (await wrap.count() > 0) {
      const w = await wrap.evaluate(el => el.getBoundingClientRect().width);
      expect(w).toBeGreaterThan(2048); // ≥80% of 2560
    }
  });
});

test.describe('M-024-D · Login 忘記密碼次要化', () => {
  test('忘記密碼 a 字色用 --c-text-3，font-size ≤ 12px', async ({ page }) => {
    await page.goto(BASE_URL + '/?view=login&onboarding=0');
    const link = page.locator('#forgot-password-link');
    await link.waitFor({ state: 'visible', timeout: 5000 });
    const meta = await link.evaluate(el => {
      const cs = getComputedStyle(el);
      return { fontSize: parseFloat(cs.fontSize), color: cs.color, td: cs.textDecorationLine };
    });
    expect(meta.fontSize).toBeLessThanOrEqual(13);
    expect(meta.td).not.toContain('underline');
  });
});

test.describe('M-024 spec-only', () => {
  test('progress label 改「第 5 步 / 共 7 步」', async ({ page }) => {
    await page.goto(BASE_URL + '/app.js');
    const text = await page.locator('body').innerText();
    expect(text).toContain('第 1 步 / 共 7 步');
  });

  test('conclusion-back-btn min-height ≥ 44 (M-008 token 蓋過)', async ({ page }) => {
    await page.goto(BASE_URL + '/?onboarding=0');
    await page.evaluate(() => {
      window.AppState.circlesPhase = 2;
      window.AppState.circlesSubmitState = 'expanded';
      window.AppState.circlesDrillStep = 'R';
      if (window.renderCirclesPhase2) window.renderCirclesPhase2();
    });
    const btn = page.locator('.conclusion-back-btn').first();
    if (await btn.count() > 0) {
      const h = await btn.evaluate(el => el.getBoundingClientRect().height);
      expect(h).toBeGreaterThanOrEqual(44);
    }
  });
});
```

- [ ] **Step 2: 確認紅燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-024-polish.spec.js \
  --reporter=line
```

Expected: 6+ tests, 多 FAIL。

---

### Task 2: systematic-debugging grep

- [ ] **Step 1: 找各子項目所在**

```bash
grep -nE "circles-info-step|@media.*1280.*display: none|forgot-password-link|circles-step-meta-row|conclusion-back-btn" public/style.css public/app.js | head -30
grep -n "progressLabel" public/app.js
grep -n "查看教練提示\|收起提示" public/app.js
```

把 hits 列下來。

---

### Task 3: M-024-A Desktop home 步驟 pill

**Files:**
- Modify: `public/style.css`

- [ ] **Step 1: 移 desktop gate**

找 `.circles-info-step` / `.circles-info-steps` 在 desktop media query 內被 `display: none` 的地方（或是反向 mobile-only 規則），改用所有 viewport 都顯示。

```css
[data-view="circles"] .circles-info-steps { display: flex; flex-wrap: wrap; gap: 6px; }
[data-view="circles"] .circles-info-step { display: inline-block; }

@media (min-width: 1280px) {
  /* 不再 hide */
  [data-view="circles"] .circles-info-steps { display: flex; }
}
```

如 grep 顯示是 `display: none` 包在 desktop block 內，把那條移除。

---

### Task 4: M-024-B 上一步重點折疊

**Files:**
- Modify: `public/app.js`（找 `buildPrevStepCardHtml` 函式）

- [ ] **Step 1: 改用 details/summary**

找 `buildPrevStepCardHtml`，把現行 div 結構改：

```js
function buildPrevStepCardHtml(stepKey) {
  // ...
  return '<details class="circles-prev-card-collapsible" data-prev-card>' +
    '<summary class="prev-card-summary">▸ 上一步重點 ' + escHtml(prevLabel) + '<span class="prev-card-hint">（點擊展開）</span></summary>' +
    '<div class="prev-card-body">' + bodyHtml + '</div>' +
  '</details>';
}
```

CSS：

```css
.circles-prev-card-collapsible { background: #f0f9ff; border-radius: 6px; padding: 6px 8px; margin-bottom: 6px; font-size: 11.5px; }
.circles-prev-card-collapsible[open] .prev-card-summary::before { content: '▾ '; }
.circles-prev-card-collapsible:not([open]) .prev-card-summary::before { content: '▸ '; }
.prev-card-summary { cursor: pointer; list-style: none; color: var(--c-text-2); font-weight: 600; }
.prev-card-summary::-webkit-details-marker { display: none; }
.prev-card-hint { color: var(--c-text-3); font-weight: normal; margin-left: 6px; font-size: 10px; }
.prev-card-body { padding-top: 6px; line-height: 1.5; color: var(--c-text-2); }
```

---

### Task 5: M-024-C NSM step 4 desktop 寬度

**Files:**
- Modify: `public/style.css`

- [ ] **Step 1: 套既有 desktop wrap cap**

找 `.nsm-step4-desktop` 規則，改成：

```css
.nsm-step4-desktop {
  max-width: min(1880px, 94vw);
  margin: 0 auto;
}
@media (min-width: 1920px) {
  .nsm-step4-desktop { max-width: min(2800px, 94vw); }
}
```

如果該 class 不存在，加到對應 NSM step 4 wrapper class 上（grep `renderNsmStep4` 找包裝 div）。

---

### Task 6: M-024-D 忘記密碼次要化

**Files:**
- Modify: `public/app.js:4676`

- [ ] **Step 1: 改 inline style**

把：
```js
'<a href="#" id="forgot-password-link" style="font-size:0.85rem;color:var(--c-primary,#1a56db);text-decoration:none">忘記密碼？</a>'
```

改成：
```js
'<a href="#" id="forgot-password-link" style="font-size:12px;color:var(--c-text-3);text-decoration:none">忘記密碼？</a>'
```

加 hover 樣式（在 style.css）：
```css
#forgot-password-link:hover { color: var(--c-primary); text-decoration: underline; }
```

---

### Task 7: Spec-only — progressLabel + meta wrap

**Files:**
- Modify: `public/app.js:240, 258, 276, 294, 312, 331, 351`
- Modify: `public/style.css`

- [ ] **Step 1: 改 progressLabel 字串**

7 處逐條：
- `'C · 澄清情境 · 1/7'` → `'C · 澄清情境 · 第 1 步 / 共 7 步'`
- `'I · 定義用戶 · 2/7'` → `'I · 定義用戶 · 第 2 步 / 共 7 步'`
- 同類推 R/C2/L/E/S。

- [ ] **Step 2: meta wrap CSS**

```css
[data-view="circles"] .circles-step-meta-row,
.circles-step-meta-row {
  display: flex;
  flex-wrap: nowrap;
  align-items: center;
  gap: 6px;
  min-width: 0;
}
.circles-step-meta-row > * {
  min-width: 0;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
```

如 selector 名不同以 grep 為準。

- [ ] **Step 3: hint copy 統一**

grep `查看教練提示\|收起提示`，確認 step I 路徑也使用同 copy。如不一致，統一改為這兩字串。

---

### Task 8: 跑測試

- [ ] **Step 1: spec 綠燈**

```bash
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/master-024-polish.spec.js \
  --reporter=line
```

- [ ] **Step 2: 全套**

```bash
npm test 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/audit-master.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
PMDRILL_BASE_URL=http://localhost:4000 npx playwright test \
  --config=tests/playwright/playwright.config.js \
  journeys/audit/rwd-visual-gate.spec.js \
  --workers=4 --reporter=line 2>&1 | tail -5
```

Expected: 三層全綠。

---

### Task 9: commit

```bash
git add public/app.js public/style.css \
  tests/playwright/journeys/audit/master-024-polish.spec.js
git commit -m "$(cat <<'EOF'
fix(circles): MASTER-024 polish 八子項

A. desktop home 顯示 .circles-info-step pill（移除 ≥1280 hide gate）
B. iPhone-SE step E 上一步重點改 details/summary 預設折疊
C. NSM step 4 desktop max-width 套 min(1880px,94vw) / min(2800px,94vw)
D. login 忘記密碼次要化（12px / --c-text-3 / hover 才轉 primary）
E-H. progressLabel 7 處改「第 N 步 / 共 7 步」+ meta-row flex-wrap nowrap
     ellipsis + hint copy 統一查看教練提示／收起提示

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

回報：commit SHA + spec pass + 三層數字。
