# Session State Cleanup — Design (2026-05-11)

> Single-purpose cleanup spec — repo hygiene after NSM 4-bug fix bundle ship (`9d0eac2`).
> User approved Approach C2 (revert baselines) on 2026-05-11.

---

## Goal

Bring repo to clean state after NSM 4-bug fix session, preserving audit deliverables and protecting visual contract baselines.

## Scope

**In scope:**
- Commit 4 new audit PNG dirs (16.8M) — referenced by 3 committed audit docs
- Commit ~250 modified historical audit PNG re-captures
- Revert 6 modified `tests/visual/baselines/` files (visual contract protection)
- `.gitignore` additions for plugin sqlite cache
- Task list cleanup — delete completed entries #1-100

**Out of scope:**
- Modified pixel-diff reports / diff PNGs (SB4-9b ship deliverables, carry-forward)
- Untracked iPhone-14/SE PNGs in `audit/png-uat-fix/` (separate batch, untouched)
- Modified `audit/pixel-diff-master-2026-05-09.md` + `audit/pixel-diff-phase-b-ship-readiness-2026-05-10.md` (review-only update from prior bundles)

## Architecture

Single bash session executing 4 sub-tasks in order:

1. **gitignore patch** — append `*.db` rule to `.gitignore`
2. **Baseline revert** — `git checkout -- tests/visual/baselines/`
3. **Audit PNG add + commit** — `git add audit/png-nsm-* audit/png-mockup-* tests/visual/diffs/` + commit
4. **Task list cleanup** — TaskUpdate to status=deleted for tasks #1-100 (preserving #101+ for current session continuity)

## Components

| Component | Responsibility | Interface |
|---|---|---|
| `.gitignore` | declare ignore patterns | append `*.db` |
| Git index | stage cleanup deltas | `git add` selective |
| Task list | clean stale tracking | TaskUpdate batch |

## Data Flow

```
working tree (dirty)
  ├─→ .gitignore patched (1 line append)
  ├─→ baselines reverted (git checkout)
  ├─→ audit PNGs staged + committed (1 commit)
  └─→ task list scrubbed (no commit, just cleanup)

→ working tree (clean, untracked = 0 except ruvector.db ignored)
```

## Error Handling

- Pre-commit hook on race regression — should still pass (no source code changes)
- If baseline revert affects in-flight work — none in flight per CLAUDE.md state board
- If `git add` exceeds 100 file limit — split into batched commits

## Testing

No new functional code, so no jest/Playwright tests needed. Verification:

- `git status --short` after = empty modified, only `?? ruvector.db` ignored
- `du -sh .git/` after = `391M + ~17M` (within accepted pattern)
- Task list = no entries in `pending` or `in_progress` for stale items

## Success Criteria

1. `git status --short` shows clean working tree (modulo `.gitignore`'d files)
2. New PNG evidence dirs committed and referenced from audit docs render correctly
3. Baseline files restored to last-committed bytes
4. Task list shows ≤ 30 active entries (down from 113)
5. No regression — `npm test` (jest 197/214) still passes

## Self-Review Checklist

- [x] No placeholders / TBD / TODO
- [x] Internal consistency — sub-tasks don't conflict
- [x] Scope check — single-purpose, no mission creep
- [x] Ambiguity check — each step has explicit command

## Approval

User approved Approach C2 (revert baselines) on 2026-05-11 chat: "a"

---

**Generated by Claude Opus 4.7 — 2026-05-11**
