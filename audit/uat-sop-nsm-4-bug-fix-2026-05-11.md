# UAT SOP — NSM History 4-Bug Fix（2026-05-11）

> commits e126f42 → 3676281 → 852be52 → 3e791ad → 1c70178 → 0c1bced
> dev server: http://localhost:4000/（已起）

---

## §A 開啟系統

1. 瀏覽器開 http://localhost:4000/
2. 以 albertpeng678@gmail.com 登入
3. 確認落在 CIRCLES 首頁（看到題目列表 + 頂部 stats strip）

---

## §B Bug 1 驗證 — 歷史記錄點擊後正確跳轉

**預期：** 點已評分 NSM 紀錄 → 直接看 Step 4 報告（舊行為：回到 Step 1 重選）

1. 點 navbar 左上 ≡ → 「練習記錄」開 offcanvas
2. 找到 **Zoom（80分）** — 右側有 "80分" badge
3. 點 Zoom 紀錄
4. **預期落點：** 直接進 NSM 報告（標題「NSM 報告 · Zoom」/ 分數 80/100 / 5 維度雷達圖）
5. **若看到 Step 1 情境選擇頁 → Bug 1 仍存在，立即回報**

Edge case：點 Fiverr（60分）同樣應進 Step 4 報告，非 Step 1。

---

## §C Bug 2 驗證 — 還原後 user_nsm 欄位有值

**預期：** 從歷史記錄還原後，Step 1 已選情境有值（舊行為：選擇為空，需重選）

1. 同 §B 步驟 1-3 點 Zoom 紀錄，進 Step 4 報告
2. 點 Step 4 頁左上 ← 返回
3. 進入 Step 1 情境選擇
4. **預期：** 頁面 header 右側顯示「已選：Zoom · SaaS 型」（或 step 顯示「已選 1」）
5. **若 header 顯示「請先選擇一個情境」且無已選 → Bug 2 仍存在**

補充驗點：offcanvas 列表中 Zoom 顯示 "80分" badge → 代表 user_breakdown 有回傳（BE Bug 2 fix 的直接證明）

---

## §D Bug 3 驗證 — Step 2 返回鍵去首頁

**預期：** 在 NSM Step 2（指標定義）按返回 → 回 CIRCLES 首頁（舊行為：回到 NSM Step 1）

1. 從 NSM 首頁選一題（任意情境）→ 點「開始 NSM 訓練」→ 進 Step 2 指標定義
2. 點 navbar 左上 ← 返回
3. **預期：** 返回 CIRCLES 首頁（看到 CIRCLES 題目列表 + "什麼是 CIRCLES 實戰訓練？" header）
4. **若回到 NSM Step 1 情境選擇 → Bug 3 仍存在**

Note：Step 1 的返回鍵本來就去首頁，只有 Step 2 的返回有 bug。

---

## §E Bug 4 驗證 — NSM 首頁顯示統計數字

**預期：** NSM 首頁（北極星指標 tab）顯示 "X 已完成 · Y 進行中 · Z 本週" stats strip（舊行為：無此區塊）

1. 點 navbar「北極星指標」→ 進 NSM Step 1 首頁
2. **預期：** 步驟列下方有一行統計：bar 圖示 + "2 已完成 · 0 進行中 · 0 本週"
3. 若已完成更多練習，數字可能不同，但格式必須有三個數值
4. **若此區塊完全不存在 → Bug 4 FE 仍存在**

---

## §F 5 分鐘冒煙（完整 NSM 一次）

1. NSM Step 1：選 Airtable → 點「開始 NSM 訓練」
2. NSM Step 2：填寫 NSM 定義（隨意填，確保有文字）→ 點「繼續到步驟 3」
3. NSM Step 3：填寫 4 個 dim breakdown → 點「送出，取得 AI 評分」
4. 等 AI 評分（5-20 秒）→ NSM Step 4 報告
5. **確認：** totalScore 在 20-100 之間 / 5 維度雷達圖出現 / dim rows 有 score 1-5
6. 點「完成」tab → 確認完成訊息
7. 回 NSM 首頁 → stats "已完成" 數字 +1

如任一步驟卡住或看到錯誤 → 截圖 + 描述，立即回報。

---

*Generated by Claude Sonnet 4.6 — 2026-05-11*
