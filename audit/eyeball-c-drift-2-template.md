# Eyeball Walk — C-Drift-2 (TEMPLATE, to be filled post-implement)

> **Purpose**: RITUAL §6 ship checklist evidence for Wave 2 commit C-Drift-2 (P0 NSM localStorage data-loss fix: D-2 + D-6).
> **Plan ref**: `audit/p2-c-drift-2-plan.md`
> **Implementer fills**: §1-§5 below. **Director cold-Reads**: §3 PNG comments. **User**: §4 UAT walk + §5 signoff.
> **Source**: GAP-7 mitigation per `audit/phase-a-prep-gaps-1-4-5-7-mitigation.md` §D.
> **Note**: this commit's primary behavior is **data persistence** — heavy on invariants, lighter on visual diff. §6 expanded.

---

## §1 Behavior coverage matrix

| # | Audit ref | Behavior under test | PNG row in §3 |
|---|---|---|---|
| 1 | D-2 (P0) | NSM session restored from history → if backend empty + localStorage fresher, local draft merged | row 1 (Step 2 form shows restored draft text) |
| 2 | D-6 | NSM 切題 reset draft → 立刻嘗試 localStorage restore (if same qid practiced before) | row 1 (same — qcard click path) |

---

## §2 Screenshot capture commands (implementer pastes output)

```bash
# 3 viewport × 1 visible behavior row = 3 PNGs total (P0 data persistence — primarily invariant-tested)
# Generated by tests/visual/capture-c-drift-2-pngs.spec.js (NEW spec — implementer creates)

npx playwright test --config tests/e2e/playwright.config.js tests/visual/capture-c-drift-2-pngs.spec.js --reporter=list

# Expected output: 3 PNG files written to audit/c-drift-2-evidence/
# Plus 2 BEFORE/AFTER pairs (qid switch) → 6 PNGs total = 3 vp × 2 states
```

Output paste (implementer):
```
<paste 3 (or 6) PNG paths here>
```

---

## §3 Director cold-Read (≥1 sentence per PNG)

### Row 1 — NSM Step 2 form populated from localStorage merge

| Viewport | PNG path | Director comment |
|---|---|---|
| desktop | `audit/c-drift-2-evidence/row1-desktop.png` | <fill: 4-dim input fields contain expected draft text, save indicator says 「已儲存」or 「儲存中…」> |
| mobile-chrome | `audit/c-drift-2-evidence/row1-mobile-chrome.png` | <fill> |
| mobile-safari | `audit/c-drift-2-evidence/row1-mobile-safari.png` | <fill> |

### (Optional) Row 2 — qid switch with prior practice → restored draft

| Viewport | PNG path | Director comment |
|---|---|---|
| desktop | `audit/c-drift-2-evidence/row2-desktop.png` | <fill: switching to a previously-practiced qid shows prior draft text> |
| mobile-chrome | `audit/c-drift-2-evidence/row2-mobile-chrome.png` | <fill> |
| mobile-safari | `audit/c-drift-2-evidence/row2-mobile-safari.png` | <fill> |

---

## §4 User UAT SOP

```
1. Start dev server: npm run dev (port 4000)
2. Login: e2e@first-principle.test

Test D-2 history restore + localStorage merge:
3. NSM tab → pick question A → Step 2 → fill 4-dim partially (e.g. reach: "abc", depth: "def")
4. Open DevTools → Application → Local Storage → verify "pmdrill:nsm:draft:<qid-A>" is written
5. Close tab (or hard refresh BEFORE auto-save PATCH completes)
6. Re-open app → navigate to offcanvas history → click the in-flight session for question A
7. EXPECT: Step 2 form populated with "abc" / "def" (from localStorage, not from backend which is empty)
8. EXPECT: console shows merge log: "[nsm-restore] localStorage fresher than backend; merging..."

Test D-2 backend wins when fresher:
9. Practice question B fully → submit gate → DB has full state
10. In DevTools, manually edit localStorage "pmdrill:nsm:draft:<qid-B>" to have older ts
11. Reload → offcanvas → click question B session
12. EXPECT: form shows backend state, NOT older localStorage state
13. EXPECT: console log indicates "backend ts newer; skipping local merge"

Test D-6 qid switch restore:
14. Practice question C → fill 4-dim → submit gate → session committed
15. Switch to question D → fill partially → close tab
16. Re-open → NSM home → click question C from q-cards
17. EXPECT: Step 2 shows question C's final state (from backend)
18. Click question D from q-cards
19. EXPECT: Step 2 shows question D's partial draft from localStorage
```

---

## §5 Mockup ↔ production pixel-diff

```
Mockup ref: docs/superpowers/specs/mockups/2026-05-02-frontend-rewrite/07-nsm-step-2-v3.html
  - Step 2 form layout for restored state

Pixel-diff command:
  npx playwright test --config tests/visual/playwright.config.js tests/visual/c-drift-2-pixel-diff.spec.js --reporter=list

Threshold: 0.5% per RITUAL §3.13

Diff report: tests/visual/diffs/c-drift-2-report.md (NEW)

Result paste:
<paste threshold pass/fail per row>
```

---

## §6 Invariants (data persistence — primary verification surface)

```
Invariant 1: localStorage WRITE on every input
- Step 2 input field → type 1 char
- EXPECT: localStorage.setItem('pmdrill:nsm:draft:<qid>', JSON.stringify({userNsm,userBreakdown,ts}))
- EXPECT: ts updates within 250ms debounce

Invariant 2: localStorage READ on history restore (NEW — this commit)
- offcanvas → click NSM session row
- EXPECT: code path hits read site at app.js line 8547-8620 NSM branch
- EXPECT: merge block executes if backend session empty OR backend ts older

Invariant 3: localStorage READ on qcard click (NEW — this commit)
- NSM home → click q-card for previously-practiced qid
- EXPECT: code path hits read site at app.js line 6457-6464
- EXPECT: merge block executes after Bug-A reset

Invariant 4: NO data loss in race scenario (P0 acceptance)
- Type 3 char in Step 2 → trigger PATCH (force 503 server response)
- EXPECT: localStorage still has the 3 chars
- EXPECT: reload → restore shows the 3 chars
- BEFORE this commit: data lost. AFTER: data persists.

Invariant 5: ts comparison correctness
- localStorage ts = 100, backend ts = 200 → use backend
- localStorage ts = 200, backend ts = 100 → use localStorage
- localStorage ts = 200, backend empty → use localStorage
- localStorage absent, backend = anything → use backend
```

---

## §7 P0 Acceptance — live Supabase verification

```
Live data check (PRE-this-ship baseline):
  nsm_sessions: lifecycle 'created' = ~999, 'gated' = ~1 (99.9% drop)

POST-this-ship monitor (1 week after deploy):
  nsm_sessions: track 'gated' / 'created' ratio change
  IF ratio > 5% → D-2 root cause confirmed amplified
  IF ratio unchanged → other amplifiers (e.g. UX confusion) at play; not D-2

Director monitors via Supabase SQL editor weekly post-ship.
```

---

## §8 User signoff

```
Director cold-Read complete: <PASS / FAIL — fill>
User UAT walk complete: <PASS / FAIL — fill>
Pixel-diff under threshold: <PASS / FAIL — fill>
Invariants 1-5 verified: <PASS / FAIL — fill>
Live data baseline captured: <PASS / FAIL — fill>

User signoff: "<對> @ <YYYY-MM-DD HH:MM>" or "<待修> + 1-line reason"

Tracker append: audit/e2e-master-tracker.md §5 — "<C-Drift-2> ship @ <SHA>"
Tracker move: §2 D-2 / D-6 entries → §5 resolved log
```
