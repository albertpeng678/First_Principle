# UAT SOP — NSM 4-bug fix bundle 親驗（commit `9d0eac2` / state board `53c7efe`）

> **Director：** 4 個 production bug 已 ship + 40 PNG cold-Read confirmed cross-vp。
> 此份 SOP 給 user 親跑驗證，覆蓋 4 個 bug 各 1 條 happy path + 1 條 edge case。
> **dev server：** `http://localhost:4000/` （已起，HTTP 200）

---

## 前置（一次性）

1. 開瀏覽器 → http://localhost:4000/
2. 登入（既有帳號或 guest）
3. 從 CIRCLES 首頁進 NSM 訓練：點 navbar 「北極星指標」按鈕

---

## Bug 2 step2 — 「範例答案」 silent-fail 修復驗證

**情境：** 老的問題沒做 backfill (q.field_examples 缺) 時，「範例答案」按鈕從前是按了沒反應（silent fail）。修復後應變灰色 disabled 狀態。

**steps：**
1. NSM 主頁 → 隨便挑一題 → 進 step 1（情境）
2. 任意 5 卡點選 → 進 step 2（指標）
3. 看「北極星指標 (NSM)」/「定義說明」/「與業務目標連結」三個 field 的 head row 右側
4. **預期：**
   - ✅ 若該題有 field_examples backfill → 「99 範例答案」navy 色可點，點下展開 panel
   - ✅ 若該題缺 field_examples（OLD snapshot）→ 「99 範例答案」**淡灰色 disabled**（opacity 0.45），按了不會 silent fail
5. **同時驗 Bug 4 fixture-only（hint）：** 「提示」button 左側依舊正常 — 點開有 modal

**邊界：** 試試從 offcanvas 開歷史記錄（左側菜單 → 練習記錄 → 點任一舊紀錄）→ Step 2 應 graceful

---

## Bug 2 step3 — 4 dim cards 「範例答案」disabled 同樣驗證

**steps：**
1. Step 2 三 field 隨意填 → 進 step 3（拆解）
2. 看 4 個 dim card：啟用廣度 / 席次深度 / 黏著頻率 / 擴張信號
3. 每張 card 底部右側：「提示」+「99 範例答案」 button row
4. **預期：** 同 Bug 2 step2 — 缺 field_examples 則 disabled，有則 navy 可點

---

## Bug 3 — 「深入分析」 fallback message 修復驗證

**情境：** 老的問題沒做 backfill (q.context 缺) 時，按「深入了解問題」展開後 4 個空白 ctx card。修復後應顯 fallback 文字。

**steps：**
1. Step 2 或 step 3 → 找到 qchip card（公司名+產業+題目情境）
2. 點 「∨ 深入了解問題」 → 卡片展開，顯「深入分析」 header
3. **預期：**
   - ✅ 若該題有 q.context backfill → 4 個 block（商業模式/使用者/常見陷阱/破題切入）顯內容
   - ✅ 若該題缺 q.context（OLD snapshot）→ 灰色 dashed border 中央 fallback：「**此題暫無深入背景資料**」
4. 不應再看到 4 個白色空 card

---

## DRIFT-A — 4-block 2x2 grid layout 驗證（mockup 06 §A LOCKED）

**情境：** 4 個 ctx block 從前是 flex column 直式堆疊。修復後 ≥768px 應 2x2 grid。

**steps：**
1. 找到一個 q.context backfill 完整的題目（new sample 大多 OK）
2. Step 2 或 step 3 → qchip 「深入了解問題」 展開
3. **預期 cross-vp：**
   - 📱 Mobile (≤767px)：4 block 直式堆疊 1 col
   - 📱📱 iPad (768px+)：2x2 grid（商業模式 ｜ 使用者 / 常見陷阱 ｜ 破題切入）
   - 🖥️ Desktop (1024px+)：同 iPad 2x2 grid
4. 拉 browser window 寬度跨 768px 邊界 → 應即時切換 layout

---

## Bug 4（fixture-only）— Step 4 完整報告 schema 驗證

**情境：** 我先前 audit fixture 用錯 schema (`dimensions.{score}` + `overall_score`)，production 一直是 `scores.{flat int}` + `totalScore` + `coachComments`。production code 沒事，**只是 fixture 自己錯了**，已修。

**steps：**
1. 隨便挑一題 → 完成 Step 1 → Step 2 → Step 3（隨便填 placeholder text 即可）
2. 點「送出，取得 AI 評分」 → 等 5-15 sec evaluation → 進 Step 4 報告
3. **預期：**
   - ✅ 大字 totalScore（20-100 之間，不是 0）
   - ✅ 5 維度雷達圖 polygon（價值關聯/領先指標/操作性/可理解性/週期敏感）
   - ✅ 5 dim score rows 每行有 score 1-5 + bar fill + coach comment 文字
   - ✅ 4 個 tab：總覽 / 對比 / 亮點 / 完成
4. **若看到 0/100 + 全 1/5 → 立即報告**（代表 production 還有 schema bug 我漏抓）

---

## 殺手鐧 — 任一發現問題請立即 ping

如以上任一條走不通 → cancel UAT，截圖 + 描述即可，不必走完。
我會立即新開 hotfix bundle。

---

**Generated by Claude Opus 4.7 — 2026-05-11**
