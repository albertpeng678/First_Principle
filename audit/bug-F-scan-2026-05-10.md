# Bug F Scan — Step Titles + Char Counter
**Date:** 2026-05-10  
**Scope:** CIRCLES Phase 1 step titles (parens) + char counter (「X / Y」text)  
**Status:** AUDIT ONLY — no code changes made

---

## Group F1: Char Counter Remove

### Render site 1 — `renderPhase1Field()` (app.js line 4152–4168)

```
app.js:4153–4165
```

**Behavior:** Field 1 only (`idx === 0`). Renders `<span class="char-counter">N / MAX</span>` inside `<div class="field__meta">`.  
**Action needed:**
- Remove `counterSpan` render entirely (lines 4153–4165: blank out or remove the block that sets `counterSpan`)
- Remove `field__meta` div when `metaSpan` is also empty (already conditional, will auto-collapse if both empty)
- Keep `is-below-floor` logic intact — the `counterClass` assignment drives the red border style; but since the span itself will be removed, the class must be applied to the textarea or wrapper instead. **Alternative:** simply omit the `counterSpan` HTML while retaining the `_isBelowFloor` computation and applying the class to the `.rt-field` container or `.rt-textarea` element.

### Render site 2 — contenteditable input handler (app.js line 6980–6989)

```
app.js:6980–6989
```

**Behavior:** On each keystroke (debounced 200ms), locates `.char-counter` span and updates `textContent` to `nonWsLen + ' / ' + max`.  
**Action needed:**
- Remove `counter.textContent = nonWsLen + ' / ' + max` update (line 6988)
- Keep `isBelowFloor` computation and `counter.className` update — **but** since span will be removed, pivot to updating class on nearest `.rt-field` or `.rt-textarea` element instead. Or if span retained (invisible), keep className-only update.

**Recommendation:** Simplest safe approach = keep `<span class="char-counter">` in DOM (invisible) but add `display:none` via CSS, so `is-below-floor` class toggle on the span still drives any border override rule. Alternatively audit whether `is-below-floor` class on the span actually drives textarea border (check CSS) — if not, add a rule that targets `.field:has(.char-counter.is-below-floor) .rt-textarea` or apply class directly to `.rt-field`.

---

## Group F2: Step Title Parens Cleanup

### CIRCLES Phase 1 — `CIRCLES_STEP_CONFIG` (app.js lines 3097–3245)

All parens are in `titleDrillSuffix` and `titleSimDesktopSuffix` fields, NOT in the base `title` field. The base `title` is already clean.

| Step | Field | Before | After |
|---|---|---|---|
| C1 | `titleDrillSuffix` (line 3118) | `（題目邊界 / 業務影響 / 假設）` | `''` (empty string) |
| I | `titleDrillSuffix` (line 3137) | `（分群 / 焦點 / 動機 / 排除）` | `''` |
| R | `titleDrillSuffix` (line 3156) | `（功能性 / 情感性 / 社交性 / 核心痛點）` | `''` |
| C2 | `titleDrillSuffix` (line 3175) | `（取捨標準 / 優先 / 暫緩 / 理由）` | `''` |
| L | `titleDrillSuffix` (line 3194) | `''` (already empty) | no change |
| L | hardcoded span (line 4319) | `（2-3 個方案）` in `renderCirclesPhase1Lstep` | remove span entirely |
| E | `titleSimDesktopSuffix` (line 3223) | `（每個方案的優缺點 / 風險 / 成功指標）` | `''` |
| S | `titleSimDesktopSuffix` (line 3245) | `（含 NSM 與 4 追蹤維度）` | `''` |

**Render paths for drill suffix (C1/I/R/C2):**
- `app.js:4944` — `stepCfg.title + stepCfg.titleDrillSuffix` concatenated; if suffix set to `''` this path auto-cleans

**Render paths for desktop sim suffix (E/S):**
- `app.js:4435–4436` — E step renderer: `escHtml(stepCfg.title) + '<span class="phase-head__title-extra">' + escHtml(stepCfg.titleSimDesktopSuffix) + '</span>'` — once suffix is `''`, the `if (isDesktop && !isDrill && stepCfg.titleSimDesktopSuffix)` guard will be false → auto-cleans
- `app.js:4602` — S step renderer: similar guard pattern → auto-cleans

**No changes needed to render logic** — clearing the suffix strings in the config is sufficient for C1/I/R/C2/E/S. L step requires removing the hardcoded span at line 4319.

### NSM Titles — No parens found

NSM phase-head titles in app.js:
- Line 1276: `定義 NSM` — no parens
- Line 1358: `NSM 品質審核` — no parens
- Line 1586: `拆解輸入指標` — no parens
- Line 5849: `選擇企業情境` — no parens

**NSM titles are clean — no action needed.**

---

## Mockup 03 — Proposed Amendment

File: `docs/superpowers/specs/mockups/2026-05-02-frontend-rewrite/03-phase-1-form.html`

### F1: Char counter in mockup

Locations with `char-counter` spans:
- Line 854: `<span class="char-counter">22 / 120</span>` — inside `field__meta`
- Line 894: `<span class="char-counter">0 / 100</span>`
- Line 1004: `<span class="char-counter">52 / 120</span>`
- Line 1120: `<span class="char-counter">82 / 120</span>` (drill view)

**Proposed amendment:** Remove all `<div class="field__meta">…</div>` blocks containing only a `char-counter` span. (4 sites)

### F2: Step title parens in mockup

Locations with parens in phase-head title:
- Line 1081: `C · 澄清情境（題目邊界 / 業務影響 / 假設）` — drill view → remove parens → `C · 澄清情境`
- Line 1382: `L · 提出方案（2-3 個方案）` — desktop sim → remove parens → `L · 提出方案`
- Line 1652: `S · 總結推薦（含 NSM 與 4 追蹤維度）` — desktop sim → remove parens → `S · 總結推薦`

All other phase-head__title occurrences are already clean (no parens).

---

## Summary of All Changes Required

### app.js (production code — pending user 放行)

| # | Location | Change |
|---|---|---|
| F1-A | Line 4153–4165 | Remove `counterSpan` HTML render (keep `_isBelowFloor` computation, apply class elsewhere) |
| F1-B | Line 6980–6989 | Remove `counter.textContent` update; keep `is-below-floor` className toggle |
| F2-C1 | Line 3118 | `titleDrillSuffix: ''` (was `'（題目邊界 / 業務影響 / 假設）'`) |
| F2-I | Line 3137 | `titleDrillSuffix: ''` (was `'（分群 / 焦點 / 動機 / 排除）'`) |
| F2-R | Line 3156 | `titleDrillSuffix: ''` (was `'（功能性 / 情感性 / 社交性 / 核心痛點）'`) |
| F2-C2 | Line 3175 | `titleDrillSuffix: ''` (was `'（取捨標準 / 優先 / 暫緩 / 理由）'`) |
| F2-L | Line 4319 | Remove hardcoded `<span class="phase-head__title-extra">（2-3 個方案）</span>` |
| F2-E | Line 3223 | `titleSimDesktopSuffix: ''` (was `'（每個方案的優缺點 / 風險 / 成功指標）'`) |
| F2-S | Line 3245 | `titleSimDesktopSuffix: ''` (was `'（含 NSM 與 4 追蹤維度）'`) |

### mockup 03 (pending user 放行)

| # | Location | Change |
|---|---|---|
| F1-M1 | Line 854 | Remove `<div class="field__meta"><span class="char-counter">22 / 120</span></div>` |
| F1-M2 | Line 894 | Remove `<div class="field__meta"><span class="char-counter">0 / 100</span></div>` |
| F1-M3 | Line 1004 | Remove `<div class="field__meta"><span class="char-counter">52 / 120</span></div>` |
| F1-M4 | Line 1120 | Remove `<div class="field__meta"><span class="char-counter">82 / 120</span></div>` |
| F2-M1 | Line 1081 | `C · 澄清情境（題目邊界 / 業務影響 / 假設）` → `C · 澄清情境` |
| F2-M2 | Line 1382 | `L · 提出方案（2-3 個方案）` → `L · 提出方案` |
| F2-M3 | Line 1652 | `S · 總結推薦（含 NSM 與 4 追蹤維度）` → `S · 總結推薦` |

---

## Karpathy Compliance

- Think Before: Scanned ALL 7 CIRCLES step configs + all NSM titles + both char-counter render paths (render + input handler) + mockup 03
- Simplicity First: Audit-only; no code touched; proposed changes minimal (string → empty, span removal)
- Surgical Changes: 9 app.js sites + 7 mockup 03 sites identified precisely by line number
- Goal-Driven: User UAT mandate satisfied — counter text gone, parens gone across all steps, is-below-floor visual logic preserved

