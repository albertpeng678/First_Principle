## Plan A baseline
- jest: 157 (156 passed + 1 skipped live OpenAI test when no real OPENAI_API_KEY in worktree env; main repo shows 157 passed with real key)
- mockup files: 17
- branch: feat/path-2-foundation
- worktree: /Users/albertpeng/Desktop/claude_project/first-principle-path2-foundation
- jest invocation in this worktree (no real .env): OPENAI_API_KEY="" SUPABASE_URL=https://test.supabase.co SUPABASE_SERVICE_ROLE_KEY=test SUPABASE_ANON_KEY=test npm test

## Plan A · Task 14 jest regression (2026-05-03)
- Total: 157 (no change from baseline)
- Passed: 133
- Skipped: 24 (PENDING_PATH_2_REIMPL — see table below)
- Failed: 0

### Skipped tests — re-enable in subsequent plans

Plan A skeleton replaced public/app.js with a thin AppState + render dispatch + renderNavbar shell.
Six legacy frontend-helper test files parsed the old app.js for symbols that no longer exist
(isStepLocked / computeStaleFlag / renderLockedBanner / renderStaleLockedBar / bindStaleActionBar
handlers / getNsmContextSource / NSM_QUESTIONS in-app declaration). Each failing describe (or
single test) was wrapped with `describe.skip` / `test.skip` plus a `PENDING_PATH_2_REIMPL`
comment. Original test bodies were left unmodified — re-enable by removing `.skip` once the
relevant Plan B/C render layer reintroduces the helper with stable signatures.

| Test file | Reason | Re-enable in |
|---|---|---|
| tests/sp1.5-helpers.test.js | isStepLocked + computeStaleFlag parsed from old app.js | Plan B |
| tests/sp1.5-locked-banner.test.js | renderLockedBanner removed | Plan B |
| tests/sp1.5-bugfix-helpers.test.js | renderStaleLockedBar + full app.js vm-parse | Plan B |
| tests/sp1.5-bugfix-action-bar.test.js | bindStaleActionBar handlers removed | Plan B |
| tests/sp4-nsm-context-prefer-pregenerated.test.js | getNsmContextSource removed | Plan C |
| tests/sp4-nsm-db-extraction.test.js (1 of 3 tests) | "no longer embeds 103-elem literal" assertion still wants `NSM_QUESTIONS =` declaration which Plan A skeleton fully removed; other 2 nsm-db.js fixture tests still pass | Plan C |
